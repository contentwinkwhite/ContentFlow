# TASKS.md — ลำดับงานสร้าง "ContentFlow"
ทำทีละ TASK เรียงลำดับ ห้ามข้าม · จบทุกก้อนต้องผ่านเช็คพอยต์ก่อนไปต่อ

## TASK 1 — Showcase ทั้งระบบ (index.html)
โบรชัวร์หน้าเดียวโชว์ทุกโมดูล (แดชบอร์ด/รีพอร์ตประจำวัน/แพลนคอนเทนต์/คลังสคริปต์/ไอเดีย&เทรนด์/ทีมงาน/สรุปรายเดือน) ธีมตาม Design System "HomeOffice" ใน CLAUDE.md
เช็คพอยต์: เลื่อนดูครบทุกจอ สวยธีมเดียวกัน มีปุ่มลิงก์ไปเปิด app.html

## TASK 2 — โครงแอพจริง (app.html)
สร้าง shell ตามสกิน HomeOffice: sidebar 7 เมนูครบ + topbar + โครง localStorage คีย์ "contentflow_data" (members, dailyReports, contentPlans, scripts, ideas) พร้อมข้อมูลตัวอย่างเริ่มต้น (สมาชิก 3-4 คน)
เช็คพอยต์: สลับเมนูได้ครบ 7 เมนู รีเฟรชแล้วข้อมูลตัวอย่างไม่หาย

## TASK 3 — โมดูลทีมงาน + รีพอร์ตประจำวัน
หน้าทีมงาน (เพิ่ม/แก้/ปิดใช้งานสมาชิก) และหน้ารีพอร์ตประจำวัน (ฟอร์มกรอกงาน + ตารางย้อนหลัง กรองตามคน/วันที่)
เช็คพอยต์: เพิ่มสมาชิก "แนน" แล้วให้แนนบันทึก "ตัดคลิป 3 ชิ้น" วันนี้ + "เขียนสคริปต์ 1 ชิ้น" เมื่อวาน → ตารางรีพอร์ตต้องโชว์ครบ 2 แถว กรองเฉพาะแนนแล้วเห็น 2 แถวนี้เท่านั้น

## TASK 4 — โมดูลแพลนคอนเทนต์
ปฏิทิน/ตารางแพลนคอนเทนต์ เพิ่มคอนเทนต์ใหม่พร้อมสถานะ วันที่ลง แพลตฟอร์ม ผู้รับผิดชอบ เปลี่ยนสถานะได้
เช็คพอยต์: สร้างคอนเทนต์ "รีวิวของกิน EP1" ลง TikTok วันที่ในอีก 3 วัน สถานะ "ไอเดีย" → เปลี่ยนเป็น "เขียนสคริปต์" → หน้าแดชบอร์ดต้องเห็นในลิสต์ "ใกล้ถึงวันลง"

## TASK 5 — โมดูลคลังสคริปต์
ฟอร์มเขียนสคริปต์ (Hook/เนื้อหา/CTA) ผูกกับ contentPlan ที่มีอยู่ + รายการสคริปต์ทั้งหมด ค้นหาได้
เช็คพอยต์: เขียนสคริปต์ผูกกับ "รีวิวของกิน EP1" จาก TASK 4 → เปิดหน้าแพลนคอนเทนต์ต้องเห็นลิงก์ไปสคริปต์นี้ได้

## TASK 6 — โมดูลไอเดีย & เทรนด์
บันทึกไอเดีย/เทรนด์/แคปชั่น ติดแท็กประเภทได้ ค้นหา/กรองตามแท็กได้
เช็คพอยต์: เพิ่มไอเดีย 2 รายการ (1 เทรนด์ + 1 แคปชั่น) → กรองตามแท็ก "เทรนด์" ต้องเหลือ 1 รายการ

## TASK 7 — แดชบอร์ด + สรุปรายเดือน + CSV
แดชบอร์ดคำนวณยอดจริงจาก dailyReports (รวม/แยกคน/แยกประเภทงาน) หน้าสรุปรายเดือนเลือกเดือนแล้วออกตาราง + ปุ่มออกรายงาน CSV
เช็คพอยต์: จากข้อมูลใน TASK 3 (แนน: ตัดคลิป 3 + เขียนสคริปต์ 1) → แดชบอร์ดต้องโชว์ยอดรวมของแนน = 4 ชิ้น แยกประเภทถูกต้อง กดออก CSV แล้วเปิดไฟล์เห็นตัวเลขตรงกัน

## TASK 8 — ตรวจรวม + ปิดงาน
ไล่ทดสอบทั้งแอพเหมือนผู้ใช้จริง: เพิ่มรีพอร์ต 5-6 วันของคนหลายคน → เช็คว่าสรุปรายเดือนรวมถูกต้องทุกจุด (แดชบอร์ด/หน้าทีมงาน/สรุปรายเดือน ตัวเลขต้องตรงกันหมด) แล้วรายงาน: ลองอะไร เจออะไร แก้อะไร

## TASK 9 — หน้าเข้าสู่ระบบ + ประวัติการใช้งาน + ส่งออกชีต
เพิ่มหน้าเข้าสู่ระบบก่อนเข้าแอพ (เลือกชื่อตัวเองจากรายชื่อสมาชิกที่ทำงานอยู่ ไม่ต้องมีรหัสผ่าน — เป็นการระบุตัวตนเพื่อบันทึก log เท่านั้น ไม่ใช่ระบบความปลอดภัยจริงจัง เพราะแอพนี้ไม่มีเซิร์ฟเวอร์หลังบ้าน) + บันทึก "ประวัติการเข้าใช้งาน" (ใครล็อกอินเมื่อไหร่) + บันทึก "ประวัติการทำงาน" (ใครเพิ่ม/แก้/ลบอะไรในระบบ เมื่อไหร่ โมดูลไหน) ทุกจุดของแอพ + เมนูใหม่ "ประวัติการใช้งาน" โชว์ทั้ง 2 ตาราง พร้อมปุ่มส่งออกเป็นไฟล์ชีต (CSV) แยกจากปุ่ม CSV ของสรุปรายเดือนเดิม
เช็คพอยต์: เปิดแอพครั้งแรก (ยังไม่เคยเลือกชื่อในแท็บนี้) ต้องเจอหน้าเข้าสู่ระบบ → เลือกชื่อ "แนน" เข้าใช้งาน → หน้าประวัติการเข้าใช้งานต้องเห็นแถวล็อกอินของแนนเพิ่มขึ้น 1 แถว → กด "รีเฟรชหน้า" ต้องยังอยู่ในระบบ (ไม่ถามชื่อซ้ำ ไม่นับล็อกอินซ้ำ) → ไปเพิ่มรีพอร์ตใหม่ 1 รายการ → หน้าประวัติการทำงานต้องเห็นแถวใหม่ระบุว่า "แนน" เป็นคนทำ พร้อมเวลาและโมดูล "รีพอร์ตประจำวัน" → กดออกรายงาน CSV ทั้ง 2 ตารางแล้วเปิดไฟล์เห็นข้อมูลตรงกัน → กด "ออกจากระบบ" แล้วต้องกลับไปหน้าเข้าสู่ระบบ โดย log เก่าทั้งหมดยังอยู่ครบ

## TASK 10 — ตั้งค่าโลโก้และชื่อแอป (แก้ไขเองได้ ไม่ต้องแก้โค้ด)
เพิ่มปุ่มแก้ไขข้าง sb-logo ใน app.html เปิด modal ตั้งค่า: เปลี่ยน "ชื่อแอป" (ข้อความ) + อัปโหลด "รูปโลโก้" ของตัวเอง (บีบอัด/ย่อขนาดภาพอัตโนมัติก่อนเก็บ กัน localStorage บวม) หรือกด "ใช้ตัวอักษรย่อแทน" เพื่อกลับไปโลโก้ "CF" ค่าเริ่มต้น — บันทึกใน settings ของ contentflow_data ตาม SPEC.md แสดงผลทันทีทั้ง sidebar และหน้าล็อกอิน
เช็คพอยต์: เปลี่ยนชื่อแอปเป็น "บีนส์มีเดีย" + อัปโหลดรูปโลโก้ 1 รูป → sidebar และหน้าล็อกอินต้องเปลี่ยนชื่อ+รูปทันที → รีเฟรชหน้าแล้วค่ายังอยู่ (ไม่หาย) → กด "ใช้ตัวอักษรย่อแทน" แล้วต้องกลับไปเป็น "CF" ปกติ

## TASK 11 — แดชบอร์ดรายคน
เพิ่มปุ่ม "📊 แดชบอร์ด" ในแถวของแต่ละสมาชิกที่หน้าทีมงาน กดแล้วเข้าหน้าโปรไฟล์การทำงานของคนนั้นคนเดียว: ยอดชิ้นงานเดือนนี้/สะสมทั้งหมด/จำนวนสคริปต์, แนวโน้มยอดงาน 6 เดือนล่าสุด, แยกตามประเภทงานเดือนนี้, รีพอร์ตล่าสุด 10 รายการ, คอนเทนต์ที่รับผิดชอบ, สคริปต์ที่เขียน — ทุกตัวเลขคำนวณจาก dailyReports/contentPlans/scripts จริงเท่านั้น มีปุ่มกลับไปหน้าทีมงาน
เช็คพอยต์: กด "แดชบอร์ด" ที่แถวของแนน → ยอดชิ้นงานเดือนนี้ต้องตรงกับที่เห็นในหน้าทีมงาน/แดชบอร์ดรวม/สรุปรายเดือน → เห็นคอนเทนต์ "รีวิวของกิน EP1" ในลิสต์คอนเทนต์ที่รับผิดชอบ (ถ้าแนนเป็นผู้รับผิดชอบ) → กดกลับไปหน้าทีมงานได้ปกติ

## TASK 12 — จัดการทีมงาน (เพิ่ม/ลบ/แก้ไขชื่อ + รูปโปรไฟล์)
เพิ่มปุ่ม "🗂️ จัดการทีมงาน" ที่หน้าทีมงาน เปิด modal แสดงรายชื่อสมาชิกทั้งหมดพร้อมปุ่ม "ลบ" ต่อคน + ฟอร์มเพิ่มสมาชิกใหม่ในหน้าเดียวกัน — ลบได้จริงเฉพาะสมาชิกที่ยังไม่มี dailyReports/contentPlans/scripts/ideas ผูกอยู่เลย (ถ้ามีข้อมูลผูกอยู่ ระบบต้องบล็อกและแนะนำให้ "ปิดใช้งาน" แทน ตามกฎเหล็กใน SPEC.md) + เพิ่มฟิลด์ "รูปโปรไฟล์" (อัปโหลดรูป บีบอัดอัตโนมัติ) ในฟอร์มเพิ่ม/แก้ไขสมาชิกเดิม แล้วให้ทุกจุดที่เคยโชว์ตัวอักษรย่อ (avatar) ในระบบ — ทีมงาน, รีพอร์ต, แพลนคอนเทนต์, ประวัติการใช้งาน, sidebar คนที่ล็อกอินอยู่, หน้าล็อกอิน — เปลี่ยนไปโชว์รูปโปรไฟล์แทนถ้ามี
เช็คพอยต์: ลองลบสมาชิกที่มีรีพอร์ตผูกอยู่ (เช่นแนน) → ต้องลบไม่ได้ มีข้อความแนะนำให้ปิดใช้งานแทน → เพิ่มสมาชิกทดสอบใหม่ไม่มีข้อมูลผูก → ลบออกได้จริงทันที (หายจากทุกจุดที่เคยมีชื่อ) → อัปโหลดรูปโปรไฟล์ให้แนน → ต้องเห็นรูปแทนตัวอักษรย่อทั้งที่หน้าทีมงาน, หน้ารีพอร์ต, sidebar (ตอนล็อกอินเป็นแนน), และหน้าล็อกอิน

## TASK 13 — แบรนด์ & ช่อง TikTok (ข้อมูลตั้งต้น)
เพิ่มโมดูล "แบรนด์" (brands ตาม SPEC.md) จัดการได้จากหน้าแดชบอร์ด: เพิ่ม/แก้ไขแบรนด์ — เก็บชื่อแบรนด์ + ช่อง TikTok ที่ดูแล (ชื่อ/ลิงก์ช่อง) + หมายเหตุ ใช้เป็นตัวเลือกผูกกับงานในแพลนงานรายวัน (TASK 14) และเป็นฐานให้แดชบอร์ดสรุปแยกแบรนด์ (TASK 15)
เช็คพอยต์: เพิ่มแบรนด์ทดสอบ 2 แบรนด์พร้อมช่อง TikTok คนละอัน → refresh แล้วข้อมูลไม่หาย → แก้ไขชื่อแบรนด์ 1 อันได้สำเร็จ

## TASK 14 — หน้าแพลนงานรายวัน
เพิ่มเมนู "แพลนงานรายวัน" ตารางบันทึกงานตามคอลัมน์ workItems ใน SPEC.md ครบทุกช่อง (เดือน, ประเภทฟอร์แมต, Content Pillar, สินค้า, รายละเอียด, ลิงก์ตัวอย่างอ้างอิง, แบรนด์, วันเริ่ม, เดดไลน์, ผู้รับผิดชอบ, สถานะ, รูปภาพ, Draft 1+คอมเมนต์, Draft 2+คอมเมนต์, Final Clip, วันที่โพสต์, ลิงก์โพสต์) ทุกงานต้องเลือกแบรนด์ที่มีอยู่จาก TASK 13 เท่านั้น (ห้ามลอย) — กรองตามแบรนด์/ผู้รับผิดชอบ/สถานะได้ เป็นหน้าที่ทีมงานเข้ามาลงงานทุกวันแทนชีตเดิม
สถานะที่ใช้ (เรียงลำดับ): ไอเดีย/วางแผน → กำลังทำ → รอตรวจ Draft 1 → แก้ไข Draft 2 → รอตรวจ Final → พร้อมโพสต์ → โพสต์แล้ว
เช็คพอยต์: เพิ่มงาน 1 ชิ้นผูกกับแบรนด์จาก TASK 13 กรอกครบทุกช่องรวมทั้ง Draft 1/Draft 2/Final Clip → เปลี่ยนสถานะไล่ลำดับได้จนถึง "โพสต์แล้ว" → กรองตามแบรนด์แล้วเห็นเฉพาะงานแบรนด์นั้น → refresh แล้วข้อมูลไม่หาย

## TASK 15 — แดชบอร์ดสรุปงานทีม (รวม + แยกแบรนด์)
อัปเดตหน้าแดชบอร์ดให้มีส่วน "สรุปงานทีม" 2 มุมมอง: (1) รวมทั้งหมดทั้งทีม (2) แยกตามแบรนด์ — การ์ดต่อแบรนด์โชว์ชื่อแบรนด์ + ช่อง TikTok ที่ลงงาน + จำนวนงานที่กำลังทำ/รอตรวจ/โพสต์แล้ว คำนวณจาก workItems.status จริงเท่านั้นตามกติกาใน SPEC.md (ห้ามพิมพ์ตัวเลขตรงๆ)
เช็คพอยต์: จากงานที่เพิ่มใน TASK 14 → แดชบอร์ดต้องเห็นแบรนด์นั้นในมุมมองแยกแบรนด์ พร้อมตัวเลขงานแต่ละสถานะตรงกับที่กรอกจริง → มุมมองรวมทั้งหมดต้องเท่ากับผลรวมของทุกแบรนด์บวกกัน

## TASK 16 — ข้อมูลพนักงานเพิ่มเติมในหน้าทีมงาน
เพิ่มฟิลด์ ตำแหน่ง, รหัสพนักงาน, วันเดือนปีเกิด, เบอร์โทรติดต่อ, อีเมล ในฟอร์มเพิ่ม/แก้ไขสมาชิก (ไม่บังคับกรอก) + แสดงข้อมูลเหล่านี้ในหน้าทีมงาน (และ/หรือแดชบอร์ดรายคนจาก TASK 11)
เช็คพอยต์: กรอกข้อมูลครบของสมาชิก 1 คน (ตำแหน่ง/รหัสพนักงาน/วันเกิด/เบอร์โทร/อีเมล) → บันทึกแล้วแสดงผลถูกต้องในหน้าทีมงาน → refresh แล้วข้อมูลไม่หาย → เพิ่มสมาชิกใหม่โดยไม่กรอกช่องเหล่านี้เลยต้องเพิ่มได้ปกติ (ไม่บังคับ)

## TASK 17 — รวมหน้ารีพอร์ต+แพลนงานรายวัน / ย้ายสรุปรีพอร์ตไปหน้าสรุปรายเดือน / สถานะพนักงานแก้ไขได้ในตาราง
สามงานในก้อนเดียว (เกี่ยวโยงกัน แก้พร้อมกัน):
1) รวมเมนู "รีพอร์ตประจำวัน" กับ "แพลนงานรายวัน" เป็นเมนูเดียวชื่อ "รายงานการทำงานประจำวัน" — หน้านี้มีฟอร์มบันทึกรีพอร์ตเร็ว (คน/วันที่/ประเภทงาน/จำนวน) อยู่ด้วยกันกับตารางแพลนงานรายวันแบบละเอียด ไม่มีตารางรีพอร์ตย้อนหลังในหน้านี้อีกต่อไป
2) หน้า "สรุปรายเดือน" เพิ่มปุ่มสลับ "รายเดือน / รายวัน" — โหมดรายวัน = เลือกวันที่ + กรองตามคน แล้วเห็นตารางรีพอร์ตของวันนั้น (ย้ายมาจากหน้ารีพอร์ตประจำวันเดิม) ลบรายการที่กรอกผิดได้ มีปุ่ม CSV ของตัวเอง
3) หน้าทีมงาน: เอาปุ่ม "ปิดใช้งาน/เปิดใช้งาน" ออก เปลี่ยนช่องสถานะในตารางเป็น dropdown แก้ไขตรงนั้นได้เลย ตัวเลือกสถานะเปลี่ยนจาก ทำงานอยู่/พ้นทีม เป็น 4 ค่า: เทสงาน / ทดลองงาน / ประจำ / พ้นทีม (ทุกจุดที่เคยเช็ค "ทำงานอยู่" ต้องหมายถึง "ไม่ใช่พ้นทีม" ทั้งหมด — ดรอปดาวน์เลือกคนบันทึกงาน, นับสมาชิกที่ทำงานอยู่, ล็อกอินได้ทุกสถานะเหมือนเดิม) ข้อมูลเก่าที่เคยเป็น active/inactive ต้อง migrate อัตโนมัติเป็น ประจำ/พ้นทีม โดยไม่ทำข้อมูลหาย
เช็คพอยต์: เมนูซ้ายเหลือรายการเดียวชื่อ "รายงานการทำงานประจำวัน" ไม่มี "รีพอร์ตประจำวัน" แยกแล้ว → บันทึกรีพอร์ตเร็วที่หน้านี้ได้ปกติ → ไปหน้าสรุปรายเดือน สลับโหมดรายวัน เลือกวันที่ที่เพิ่งบันทึก ต้องเห็นแถวที่เพิ่งกรอก → กลับไปหน้าทีมงาน เปลี่ยนสถานะแนนจาก dropdown เป็น "ทดลองงาน" ทันทีไม่ต้องเปิดฟอร์ม → แนนยังเลือกได้ในดรอปดาวน์บันทึกรีพอร์ต/งาน (เพราะไม่ใช่พ้นทีม) → เปลี่ยนเป็น "พ้นทีม" แล้วแนนต้องหายจากดรอปดาวน์เลือกคน แต่ตัวเลขย้อนหลังของแนนในสรุปรายเดือน/แดชบอร์ดต้องยังอยู่ครบ → รีเฟรชข้อมูลทั้งหมดไม่หาย

## TASK 18 — เปลี่ยนชื่อสรุปรายเดือน / หน้าปฏิทิน / แก้โปรไฟล์จากหน้าล็อกอิน / หน้างานที่บริษัทรับผิดชอบ
สี่งานในก้อนเดียว (เกี่ยวโยงกัน แก้พร้อมกัน):
1) เปลี่ยนชื่อเมนู "สรุปรายเดือน" เป็น "สรุปรีพอร์ต" (data-page เดิม ไม่เปลี่ยน แค่ป้ายชื่อในไซด์บาร์/หัวข้อ/topbar)
2) เพิ่มเมนู "ปฏิทิน" ใหม่ — ตารางปฏิทินรายเดือน 7 คอลัมน์ (อา-ส) มีปุ่มเลื่อนเดือนก่อน/ถัดไป แต่ละวันโชว์รายการคอนเทนต์จากแพลนคอนเทนต์ (publishDate ตรงวันนั้น สีชิปตามสถานะ) และงานจากแพลนงานรายวัน (deadline ตรงวันนั้น) แบบย่อ
3) เพิ่มเมนู "งานที่บริษัทรับผิดชอบ" — ย้ายการจัดการแบรนด์ (เพิ่ม/แก้ไข/ลบ) จากการ์ดในหน้าแดชบอร์ดมาเป็นหน้าแยกของตัวเอง หน้าแดชบอร์ดเหลือแค่สรุปตัวเลขแยกแบรนด์ (อ่านอย่างเดียว) พร้อมลิงก์มาหน้านี้ + เพิ่มดรอปดาวน์ "แบรนด์ (ไม่บังคับ)" ในฟอร์มรีพอร์ตเร็วที่หน้ารายงานการทำงานประจำวัน (dailyReports มีฟิลด์ brandId ใหม่) + เพิ่มส่วน "ชิ้นงานแยกตามแบรนด์ (เดือนนี้)" บนแดชบอร์ด คำนวณจาก dailyReports.brandId จริง
4) หน้าเข้าสู่ระบบ: แต่ละแถวโชว์ตำแหน่ง (position) ใต้ชื่อ + เพิ่มปุ่ม "✎" แก้ไขโปรไฟล์ (เปิด modal เดียวกับหน้าทีมงาน — ชื่อ/รูป/ตำแหน่ง ฯลฯ) ได้ก่อนล็อกอิน
เช็คพอยต์: เมนูซ้ายมี "สรุปรีพอร์ต" "ปฏิทิน" "งานที่บริษัทรับผิดชอบ" ครบ → หน้าปฏิทินเห็นคอนเทนต์ "รีวิวของกิน EP1" ในวันที่ตรงกับ publishDate → เพิ่มรีพอร์ตใหม่พร้อมเลือกแบรนด์ที่หน้ารายงานการทำงานประจำวัน → แดชบอร์ดเห็นยอดขึ้นในส่วน "ชิ้นงานแยกตามแบรนด์" ตรงกับที่กรอก → หน้าล็อกอิน เห็นตำแหน่งใต้ชื่อ กดปุ่ม ✎ แก้ไขชื่อ/รูปคนหนึ่งได้สำเร็จ แล้วชื่อ/รูปใหม่ต้องอัปเดตในลิสต์ทันที → รีเฟรชข้อมูลทั้งหมดไม่หาย

## TASK 19 — ปรับโครงสร้างแพลนงานรายวันใหม่ทั้งหมด (ฟิลด์ตรงฟอร์มจริงของทีม)
ปรับ workItems ให้ตรงกับฟอร์มที่ทีมใช้จริง แทนโครงสร้างเดิมของ TASK 14:
1) เพิ่มฟิลด์ "หัวข้องาน" (title) พิมพ์เองได้อิสระ ไม่บังคับ — ใช้แทน product เป็นชื่อหลักที่โชว์ในตาราง (ตอบโจทย์ "แก้ไขชื่อข้อหัวได้")
2) ฟิลด์ในฟอร์ม/ตาราง เรียงใหม่: วันที่ * · ทีมงาน (ownerId) * · แบรนด์ (brandId) * · Format Type (ไม่บังคับ) · Content Pillar (ไม่บังคับ) · Product * (บังคับ เลือกจากสินค้าของแบรนด์ที่เลือกไว้เท่านั้น) · รายละเอียด (description) · สถานะ · รูปภาพ (imageNote) * · ลิงก์ไดรฟ์งาน (driveLink, ฟิลด์ใหม่รวมลิงก์เดียวแทน Draft1/Draft2/FinalClip เดิม) *
3) เอาฟิลด์เดือน/วันเริ่ม/เดดไลน์/ลิงก์อ้างอิง/Draft1+Draft2 (ลิงก์+คอมเมนต์)/Final Clip ออกจากฟอร์ม (แทนที่ด้วยข้อ 1-2) — เขียน migration ให้ข้อมูลเก่าที่เคยกรอกไว้ไม่หาย (deadline/startDate → date, ลิงก์ล่าสุดที่มีค่า → driveLink)
4) สถานะเปลี่ยนเป็น 6 ค่า: ดำเนินการ / รอตรวจ / แก้ไข / สำเร็จ / Post / ยกเลิก (migrate สถานะเก่าอัตโนมัติตาม mapping ใน SPEC.md) — เปลี่ยนสถานะเป็น "Post" จาก dropdown ในตารางต้องเด้งกล่องเล็กถามวันที่โพสต์+ลิงก์โพสต์ทันที บันทึกโดยไม่ต้องเปิดฟอร์มเพิ่มงานใหม่ แก้ไขวันที่/ลิงก์ภายหลังได้จากปุ่มเดิมซ้ำ
5) Format Type และ Content Pillar เปลี่ยนจากลิสต์ตายตัว/ช่องพิมพ์อิสระ เป็นรายการที่จัดการเพิ่ม/แก้ไข/ลบได้จากหน้า "งานที่บริษัทรับผิดชอบ" (ค่าเริ่มต้นตามที่กำหนด) แก้ไขชื่อแล้วงานเก่าที่อ้างชื่อเดิมต้องอัปเดตตามด้วย ไม่ใช่ค้างชื่อเก่า
6) หน้า "งานที่บริษัทรับผิดชอบ" เพิ่มช่องจัดการ "รายชื่อสินค้า" ต่อแบรนด์ (เพิ่ม/แก้ไข/ลบ) ในฟอร์มแก้ไขแบรนด์เดิม — dropdown Product ในฟอร์มรายงานการทำงานต้องกรองตามแบรนด์ที่เลือกไว้เท่านั้น เปลี่ยนแบรนด์แล้ว Product ต้องรีเซ็ตตาม
7) อัปเดตปฏิทิน (TASK 18) ให้อ่านจาก workItems.date แทน deadline เดิม
เช็คพอยต์: เพิ่มแบรนด์ใหม่พร้อมสินค้า 2 รายการ → เพิ่มงานใหม่เลือกแบรนด์นั้น dropdown Product ต้องเห็นแค่สินค้าของแบรนด์นั้น กรอกครบทุกช่องบังคับแล้วบันทึกได้ → เปลี่ยนสถานะงานเป็น "Post" จากตาราง ต้องเด้งถามวันที่โพสต์+ลิงก์ทันทีโดยไม่เปิดฟอร์มเพิ่มงาน → เปลี่ยนชื่อ Format Type ที่หน้างานที่บริษัทรับผิดชอบ แล้วงานที่เคยเลือกชื่อเดิมต้องอัปเดตชื่อใหม่ตาม → ข้อมูลงานเก่าจาก TASK 14 (ก่อน migrate) ต้องยังอยู่ครบ มี date/driveLink/สถานะใหม่ที่ map มาถูกต้อง → refresh ข้อมูลไม่หาย

## TASK 20 — ปรับหน้าเข้าสู่ระบบใหม่ + เพิ่มฟิลด์แผนก
1) หน้าเข้าสู่ระบบ: เอาปุ่ม "✎" แก้ไขโปรไฟล์ที่ติดอยู่ทุกแถว (จาก TASK 18) ออก เหลือแค่คลิกชื่อ/รูปเพื่อล็อกอินอย่างเดียวต่อแถว — แทนที่ด้วยปุ่มเดียว "จัดการโปรไฟล์ผู้ใช้" ที่วางไว้ด้านล่างสุดของการ์ดล็อกอิน กดแล้วเปิด modal "จัดการทีมงาน" เดิม (ใช้ modal เดียวกับหน้าทีมงาน ไม่ต้องสร้างใหม่ซ้ำ) — เปิดได้ก่อนล็อกอินเหมือนเดิม
2) เพิ่มฟิลด์ "แผนก" (department) ให้ members — ไม่บังคับกรอก เหมือนตำแหน่ง/รหัสพนักงาน/วันเกิด/เบอร์โทร/อีเมล เพิ่มช่องนี้ในฟอร์มแก้ไขสมาชิกแบบเต็ม (memberModal) และในตารางหน้าทีมงาน
3) ฟอร์ม "เพิ่มสมาชิกใหม่" ใน modal จัดการทีมงาน (quick add) เดิมมีแค่ชื่อเล่น+วันที่เข้าร่วม — เพิ่มช่อง "ตำแหน่ง" และ "แผนก" เข้าไปด้วย (ยังไม่บังคับกรอกเหมือนเดิม) กรอกตอนเพิ่มสมาชิกใหม่ได้เลยโดยไม่ต้องเปิดฟอร์มแก้ไขแบบเต็มซ้ำอีกรอบ
4) แต่ละแถวในลิสต์ของ modal จัดการทีมงาน เพิ่มปุ่ม "แก้ไข" (เปิด memberModal แบบเต็มของคนนั้น) คู่กับปุ่ม "ลบ" เดิม
เช็คพอยต์: เปิดหน้าเข้าสู่ระบบ (ยังไม่ล็อกอิน) เห็นปุ่ม "จัดการโปรไฟล์ผู้ใช้" ปุ่มเดียวด้านล่าง ไม่มีปุ่ม ✎ ติดแถวแล้ว → กดปุ่มนั้น เพิ่มสมาชิกใหม่ 1 คนพร้อมกรอกชื่อ/ตำแหน่ง/แผนก → ปิด modal กลับมาหน้าล็อกอิน เห็นชื่อใหม่ในลิสต์ทันที → เปิด modal อีกครั้ง กด "แก้ไข" คนที่เพิ่งเพิ่ม แก้แผนกเป็นค่าใหม่ บันทึกสำเร็จ → ไปหน้าทีมงาน เห็นแผนกที่แก้ในตารางตรงกัน → รีเฟรชข้อมูลไม่หาย

## TASK 21 — จัดกึ่งกลางแถวรายชื่อในหน้าเข้าสู่ระบบ
หน้าเข้าสู่ระบบ: แต่ละแถวรายชื่อสมาชิก (รูป+ชื่อ+ตำแหน่ง) ปัจจุบันชิดซ้ายของปุ่ม เหลือพื้นที่ว่างขวามือเยอะ — ปรับให้กลุ่มรูป+ชื่อ+ตำแหน่งอยู่กึ่งกลางแถวแทน (ทั้งแถวปุ่มล็อกอินและปุ่ม "จัดการโปรไฟล์ผู้ใช้" ด้านล่างก็จัดกึ่งกลางอยู่แล้ว ไม่ต้องแก้)
หมายเหตุ: ส่วนอื่นที่ขอมาพร้อมกัน (แก้ไขชื่อข้อหัวได้ + โครงสร้างฟอร์มรายงานการทำงาน 10 ข้อ ทั้งหมด) ตรงกับที่ทำไปแล้วครบใน TASK 19 (ทดสอบผ่านแล้ว) จึงไม่ต้องทำซ้ำ
เช็คพอยต์: เปิดหน้าเข้าสู่ระบบ รูป+ชื่อ+ตำแหน่งของแต่ละคนอยู่กึ่งกลางแถว ไม่เอียงซ้าย

## TASK 22 — รวมฟอร์ม "บันทึกรีพอร์ตวันนี้ (เร็ว)" กับ "เพิ่มงาน" เป็นฟอร์มเดียว
หน้ารายงานการทำงานประจำวันมี 2 ฟอร์มแยกกันซ้ำซ้อน (กรอกงานเดียวกันสองที) — รวมเป็นฟอร์มเดียว กดบันทึกครั้งเดียวได้ทั้งยอดรีพอร์ต (นับจำนวนชิ้นรายเดือน) และงานละเอียด (ผูกแบรนด์/สถานะ):
1) เอาการ์ด "บันทึกรีพอร์ตวันนี้ (เร็ว)" และปุ่ม "➕ เพิ่มงาน" (โมดัลเพิ่มงานแบบเดิม) ออก แทนที่ด้วยการ์ดฟอร์มเดียวต้นหน้า มีฟิลด์ครบ: วันที่* · ทีมงาน* · แบรนด์* · ประเภทงาน* (ตัดคลิป/ถ่ายฟุตเทจ/ออกกอง/พากย์เสียง/เขียนสคริปต์/อื่นๆ) · จำนวนชิ้น* · Format Type · Content Pillar · Product* (กรองตามแบรนด์) · รายละเอียด/หมายเหตุ · สถานะ (default ดำเนินการ) · Image* · ลิงก์ไดรฟ์งาน*
2) กด "บันทึกงาน" ครั้งเดียว สร้าง 2 บันทึกที่ผูกกัน: รายการใน workItems (แบบละเอียด) + รายการใน dailyReports (ประเภทงาน+จำนวนชิ้น สำหรับสรุปรายเดือน) — เชื่อมกันด้วย workItems.reportId → dailyReports.id ถ้าเลือกสถานะ "Post" ทันทีตอนบันทึก ให้เด้งถามวันที่โพสต์+ลิงก์ทันทีเหมือนเดิม
3) ตารางแพลนงานรายวัน (ละเอียด) ที่เหลืออยู่ ปุ่ม "แก้ไข" ต่อแถว เปิดฟอร์มเดิมที่มีฟิลด์ครบ (รวมประเภทงาน+จำนวนชิ้นด้วย) แก้แล้วอัปเดตทั้ง workItem และ dailyReport ที่ผูกกันพร้อมกัน (ถ้ารายการเก่าก่อน TASK 22 ยังไม่มี dailyReport ผูกอยู่ ให้สร้างใหม่ตอนแก้ไขครั้งแรก)
4) ปุ่ม "ลบ" ต่อแถวในตารางแพลนงานรายวัน ต้องลบ dailyReport ที่ผูกกันไปด้วย (ไม่ทิ้งขยะ) — และปุ่ม "ลบ" ที่หน้าสรุปรีพอร์ต (โหมดรายวัน) ต้องลบ workItem ที่ผูกกันไปด้วยเช่นกัน (ลบด้านไหนก็ลบทั้งคู่)
5) หน้าสรุปรีพอร์ต (โหมดรายวัน/รายเดือน) และ CSV เดิมยังทำงานเหมือนเดิมทุกอย่าง เพราะยังอ่านจาก dailyReports เหมือนเดิม ไม่ต้องแก้ตรงนั้น
เช็คพอยต์: กรอกฟอร์มเดียวครบทุกช่องบังคับ บันทึกครั้งเดียว → เห็นแถวใหม่ทั้งในตารางแพลนงานรายวัน และในหน้าสรุปรีพอร์ต (โหมดรายวัน) ตรงกัน → ยอดชิ้นงานเดือนนี้ที่แดชบอร์ด/หน้าทีมงานขยับตามจำนวนชิ้นที่กรอก → แก้ไขงานนั้นจากตาราง เปลี่ยนจำนวนชิ้น บันทึก แล้วตัวเลขสรุปอัปเดตตาม → ลบงานนั้นจากตาราง แล้วรายการที่หน้าสรุปรีพอร์ตหายไปด้วย → รีเฟรชข้อมูลไม่หาย

## TASK 23 — เพิ่มงานจากหน้าปฏิทินได้โดยตรง
หน้าปฏิทินตอนนี้ดูอย่างเดียว แก้ไม่ได้ — เพิ่มทางเข้าฟอร์ม "เพิ่มงาน" (โมดัลเดียวกับปุ่มแก้ไขในตารางแพลนงานรายวัน จาก TASK 22 ที่มีฟิลด์ครบวันที่/ทีมงาน/แบรนด์/ประเภทงาน/จำนวนชิ้น/Format Type/Content Pillar/Product/รายละเอียด/สถานะ/Image/ลิงก์ไดรฟ์) ให้เข้าถึงได้ตรงจากหน้าปฏิทิน 2 ทาง:
1) ปุ่ม "➕ เพิ่มงาน" ที่หัวหน้า — เปิดฟอร์มเพิ่มงานใหม่ พรีฟิลวันที่เป็นวันนี้ (ถ้าเดือนที่ดูอยู่ตรงกับเดือนปัจจุบัน) หรือวันที่ 1 ของเดือนที่กำลังดูอยู่ (ถ้าเลื่อนไปเดือนอื่น)
2) คลิกที่ช่องวันที่ในตาราง (นอกเหนือจากชิปงานที่มีอยู่แล้ว) — เปิดฟอร์มเพิ่มงานใหม่ พรีฟิลวันที่เป็นวันที่ที่คลิกทันที ไม่ต้องพิมพ์วันที่เอง
3) คลิกที่ชิปงาน (📋) ที่มีอยู่แล้วในวันนั้น ต้องเปิดฟอร์มแก้ไขงานชิ้นนั้น (ไม่ใช่เพิ่มงานใหม่ทับ) — ต้องไม่ทริกเกอร์การเพิ่มงานใหม่ซ้อนจากการคลิกทะลุไปโดนช่องวันที่ด้านหลัง
บันทึกงานใหม่จากหน้านี้ต้องสร้างทั้ง workItem และ dailyReport ที่ผูกกันเหมือนช่องทางอื่นทุกประการ (ตาม TASK 22)
เช็คพอยต์: หน้าปฏิทิน คลิกวันที่ว่างวันหนึ่ง (ไม่มีงาน) ฟอร์มเพิ่มงานเด้งขึ้นพร้อมวันที่ตรงกับที่คลิกไว้ล่วงหน้า → กรอกครบทุกช่องบังคับแล้วบันทึก → กลับมาหน้าปฏิทิน เห็นชิปงานใหม่ขึ้นในวันนั้นทันที → คลิกปุ่ม "➕ เพิ่มงาน" ที่หัวหน้าเดือน ฟอร์มเปิดพร้อมวันที่วันนี้ (หรือวันที่ 1 ถ้าเลื่อนดูเดือนอื่นอยู่) → คลิกชิปงานที่มีอยู่แล้ว ต้องเปิดฟอร์มแก้ไขงานชิ้นนั้น ไม่ใช่ฟอร์มเพิ่มใหม่ → เช็คหน้ารายงานการทำงานประจำวันและหน้าสรุปรีพอร์ต เห็นงานที่เพิ่มจากปฏิทินขึ้นตรงกัน → รีเฟรชข้อมูลไม่หาย

## TASK 24 — ตรวจรวมทั้งระบบรอบสุดท้าย + แก้บั๊กสถานะ Post ค้างตอนกดยกเลิก
ไล่ทดสอบทั้งแอพเหมือนผู้ใช้จริงตาม TASK 1-23 ทุกเมนู (ล็อกอิน/รายงานการทำงาน/ปฏิทิน/แบรนด์/ทีมงาน/สรุปรีพอร์ต/แดชบอร์ด) รวม edge case (ช่องว่าง, ค่าติดลบ/ศูนย์, ภาษาไทย+วรรณยุกต์+อีโมจิ, XSS, กดซ้ำ, ข้อมูลจำนวนมาก, refresh/แท็บใหม่)
บั๊กที่เจอและแก้แล้ว: เปลี่ยนสถานะงานเป็น "Post" (จาก dropdown ในตาราง, จากฟอร์มบันทึกงานวันนี้, หรือจากฟอร์มแก้ไขงาน) แล้วกด "ยกเลิก" ในกล่องที่เด้งถามวันที่โพสต์/ลิงก์ — เดิมสถานะจะค้างเป็น "Post" ทั้งที่ไม่มีวันที่/ลิงก์โพสต์ เพราะระบบ commit สถานะ "Post" ลง data ไปก่อนเปิดกล่องถามแล้ว ไม่มีจังหวะ revert เวลากดยกเลิก — แก้โดยไม่ commit สถานะ "Post" ลง data จนกว่าจะกดบันทึกในกล่องถามวันที่โพสต์/ลิงก์สำเร็จเท่านั้น (ถ้ายกเลิก สถานะเดิมต้องไม่เปลี่ยน หรือกรณีเพิ่ม/แก้ไขงานพร้อมเลือกสถานะ Post ทันที ให้บันทึกด้วยสถานะก่อนหน้า/ค่าเริ่มต้นไปก่อน)
เช็คพอยต์: เปลี่ยนสถานะงานเป็น "Post" จากตาราง กด "ยกเลิก" ในกล่องที่เด้งขึ้น → สถานะต้องกลับเป็นค่าเดิมทันที ไม่ค้างเป็น Post → เพิ่มงานใหม่พร้อมเลือกสถานะ Post ตั้งแต่ตอนกรอกฟอร์ม กด "ยกเลิก" ในกล่องถามวันที่โพสต์ → งานที่เพิ่งสร้างต้องมีสถานะเป็นค่าเริ่มต้น (ดำเนินการ) ไม่ใช่ Post → กดยืนยันบันทึกวันที่โพสต์/ลิงก์ตามปกติ (ไม่กดยกเลิก) ต้องยังใช้งานได้ถูกต้องเหมือนเดิมทุกจุด

## TASK 25 — ย้ายที่เก็บข้อมูลจาก localStorage เครื่องเดียว ไปเป็น Supabase กลาง (ใช้จากมือถือทีมงานได้ เห็นข้อมูลชุดเดียวกัน)
ทีมงานอยากใช้แอปจากมือถือของตัวเอง เห็นข้อมูลชุดเดียวกับที่หัวหน้าดูในคอม — localStorage เดิมทำแบบนี้ไม่ได้เพราะแยกเก็บต่อเบราว์เซอร์/เครื่อง ต้องมีที่เก็บกลางบนอินเทอร์เน็ต
สถาปัตยกรรมที่เลือก (จงใจเลือกแบบง่ายก่อน ไม่ใช่ full relational): ตาราง Supabase เดียวชื่อ `contentflow_data` แถวเดียว (id='main') คอลัมน์ `payload` เป็น jsonb เก็บก้อนข้อมูลเดิมทั้งหมด (members/brands/workItems/dailyReports/contentPlans/scripts/ideas/sessions/activityLog/settings/formatTypes/contentPillars) แบบเดียวกับที่เคยอยู่ใน localStorage เป๊ะ — รักษาโครงสร้างข้อมูลและ logic เดิมทั้งหมดจาก TASK 1-24 ไว้ครบ เปลี่ยนแค่ "ที่เก็บ" จาก localStorage เป็น fetch() ไป Supabase REST API
1) รัน SQL สร้างตาราง + trigger updated_at + RLS policy (ให้ผู้ใช้รันเองใน Supabase SQL Editor เพราะต้องใช้สิทธิ์ระดับโปรเจกต์ที่ anon key ทำไม่ได้)
2) เปลี่ยน `loadData()` เป็น async ดึงจาก Supabase (`GET /rest/v1/contentflow_data?id=eq.main`) แทน `localStorage.getItem` — ครั้งแรกที่ยังไม่มีแถวข้อมูล ให้ seed ค่าเริ่มต้นแล้วส่งขึ้นไปสร้างแถวให้ทันที — คง migration logic เดิมทั้งหมด (member status, workItems, formatTypes ฯลฯ) ไว้ใช้กับ payload ที่ดึงมา
3) เปลี่ยน `saveData()` เป็น PATCH ข้อมูลทั้งก้อนขึ้น Supabase แทน `localStorage.setItem` — ฟังก์ชันอื่นๆ ที่เรียก `saveData()` ทั้งหมด (save/delete ทุกโมดูล) ไม่ต้องแก้ เพราะยังคงเรียกแบบเดิม (fire-and-forget ไม่ต้อง await เพราะ UI อัปเดตจาก `data` ในหน่วยความจำอยู่แล้ว)
4) เพิ่มหน้าจอ "กำลังโหลดข้อมูล..." ระหว่างรอ fetch ครั้งแรกตอนเปิดแอป (ก่อนหน้านี้ localStorage อ่านได้ทันที ไม่มีจังหวะรอ) และหน้าจอ error พร้อมปุ่ม "ลองใหม่" ถ้าต่อ Supabase ไม่ได้ (เน็ตหลุด/URL หรือ key ผิด)
5) กันข้อมูลชนกัน: ก่อนบันทึกทุกครั้งเช็ค `updated_at` ของแถวกับค่าที่ดึงมาตอนโหลดล่าสุด/บันทึกล่าสุด ถ้าไม่ตรง (มีคนอื่นบันทึกไปก่อนแล้ว) ให้เตือนผู้ใช้ "ข้อมูลถูกแก้ไขจากที่อื่น กรุณารีเฟรชหน้าก่อนบันทึกซ้ำ" แทนการทับเงียบๆ
6) เพิ่มปุ่ม "🔄 รีเฟรชข้อมูล" ที่ topbar (โหลด payload ล่าสุดจาก Supabase มาแทนที่ `data` ในเครื่อง แล้ว re-render ทุกหน้า) + auto refetch ทุกครั้งที่กลับมาโฟกัสแท็บ (visibility change) เพื่อให้เห็นข้อมูลจากเครื่องอื่นโดยไม่ต้องกดเองตลอด
7) อัปเดต CLAUDE.md/SPEC.md ให้ตรงกับสถาปัตยกรรมใหม่ (ทำไปแล้วในก้อนนี้)
เช็คพอยต์: รันแอปสองแท็บ/สองเครื่องพร้อมกัน (จำลอง desktop + มือถือ) → เพิ่มรีพอร์ตจากแท็บ A → กดรีเฟรชที่แท็บ B เห็นรายการใหม่ทันที → ปิดเบราว์เซอร์ทั้งคู่ เปิดใหม่ ข้อมูลยังอยู่ครบ (มาจาก Supabase ไม่ใช่ localStorage แล้ว) → ลองบันทึกพร้อมกันสองแท็บโดยตั้งใจ (ไม่รีเฟรชแท็บที่สองก่อน) ต้องเจอข้อความเตือนชนกัน ไม่ใช่ข้อมูลหายเงียบๆ → ปิดเน็ต/ใส่ key ผิดทดสอบ ต้องเห็นหน้าจอ error ที่กดลองใหม่ได้ ไม่ใช่หน้าขาวค้าง

## TASK 26 — ผ่อนฟิลด์บังคับในฟอร์มบันทึกงาน ตามประเภทงาน
เดิมฟอร์มบันทึกงาน (ทั้งฟอร์มเร็วหน้ารายงานการทำงาน และฟอร์มแก้ไขงาน) บังคับกรอก Product/Image/ลิงก์ไดรฟ์งาน เสมอไม่ว่าประเภทงานจะเป็นอะไร — แต่ในทางปฏิบัติมีแค่ "ตัดคลิป" เท่านั้นที่มีชิ้นงาน (คลิปสำเร็จ) ให้อ้างอิงจริง ส่วนประเภทอื่น (ออกกอง, ถ่ายฟุตเทจ, พากย์เสียง, เขียนสคริปต์, อื่นๆ) เป็นงานขั้นต้นที่ยังไม่มีคลิป/รูป/ลิงก์ให้ใส่
1) Product/Image/ลิงก์ไดรฟ์งาน บังคับกรอก (มี *) เฉพาะตอนเลือกประเภทงาน = "ตัดคลิป" เท่านั้น — ประเภทงานอื่นทั้งหมด ฟิลด์เหล่านี้กลายเป็นไม่บังคับ (เครื่องหมาย * หายไปจาก label โดยอัตโนมัติเมื่อเปลี่ยนประเภทงาน) เหลือแค่วันที่/ทีมงาน/แบรนด์/ประเภทงาน/จำนวนชิ้น ที่ยังบังคับเสมอ ช่องรายละเอียด/หมายเหตุ ไม่บังคับเหมือนเดิม
2) ดรอปดาวน์ Product ตอนไม่บังคับ มีตัวเลือก "— ไม่ระบุ —" ให้เลือกปล่อยว่างได้ (ตอนบังคับ ถ้าแบรนด์ยังไม่มีสินค้าจะเตือนให้ไปเพิ่มสินค้าก่อนเหมือนเดิม)
3) ใช้ได้ทั้งฟอร์มเพิ่มงานเร็วหน้ารายงานการทำงาน และฟอร์มแก้ไขงาน (ต้องอัปเดต label ให้ตรงตั้งแต่เปิดฟอร์มแก้ไขงานเก่าที่ประเภทไม่ใช่ตัดคลิปด้วย ไม่ใช่แค่ตอนเปลี่ยนประเภทงานสดๆ)
เช็คพอยต์: เปิดฟอร์มเพิ่มงาน เลือกประเภทงาน "ออกกอง" → เครื่องหมาย * ที่ Product/Image/ลิงก์ไดรฟ์งาน หายไป → เว้นว่างทั้งสามช่อง กรอกแค่รายละเอียด แล้วบันทึกได้สำเร็จ → เปลี่ยนกลับเป็น "ตัดคลิป" แล้วลองบันทึกโดยไม่กรอก Product ต้องถูกบล็อกเหมือนเดิม → เปิดแก้ไขงานเก่าที่เคยเป็นประเภท "ออกกอง" เครื่องหมาย * ต้องหายไปตั้งแต่เปิดฟอร์ม ไม่ใช่ต้องกดเปลี่ยนประเภทงานก่อนถึงจะอัปเดต

## TASK 27 — ยกเลิกบังคับ Image และลิงก์ไดรฟ์งาน ทุกประเภทงาน (รวมตัดคลิป)
ต่อจาก TASK 26 — ทีมงานอยากให้ Image กับลิงก์ไดรฟ์งาน ไม่บังคับกรอกเลย ไม่ว่าจะเลือกประเภทงานไหนก็ตาม (รวมถึง "ตัดคลิป" ที่ยังบังคับอยู่จาก TASK 26 ด้วย) เพราะบางทีต้องรีบลงงานก่อน ไฟล์/ลิงก์ยังไม่พร้อมตอนนั้น
1) เอาเครื่องหมาย * และการเช็คบังคับของ Image (imageNote) และลิงก์ไดรฟ์งาน (driveLink) ออกทั้งหมด ทุกประเภทงาน — label เปลี่ยนเป็น "(ไม่บังคับ)" แบบข้อความนิ่งตลอด ไม่ต้องสลับตามประเภทงานอีกต่อไป
2) Product (สินค้า) ยังคงกติกาเดิมจาก TASK 26 ไว้ (บังคับเฉพาะตอนประเภทงาน = "ตัดคลิป" เท่านั้น) — ไม่ได้ขอให้เอาออกด้วย
3) ใช้ผลกับทั้งฟอร์มเพิ่มงานเร็วและฟอร์มแก้ไขงาน
เช็คพอยต์: เลือกประเภทงาน "ตัดคลิป" เว้นว่าง Image และลิงก์ไดรฟ์งาน (แต่เลือก Product ไว้) บันทึกได้สำเร็จ ไม่มีข้อความเตือน → ลองเว้นว่าง Product ตอนเลือก "ตัดคลิป" ยังคงถูกบล็อกเหมือนเดิม (ไม่ได้เปลี่ยน)

## TASK 28 — ยกเลิกบังคับ Product ด้วย (เอาที่บังคับใส่ออกทั้งหมด)
ต่อจาก TASK 27 — Product ยังเหลือเป็นฟิลด์บังคับตอนประเภทงาน = "ตัดคลิป" อยู่ ทีมงานขอให้เอาออกด้วยเลย ตอนนี้ Product/Image/ลิงก์ไดรฟ์งาน ไม่บังคับกรอกทั้งหมด ทุกประเภทงาน — เหลือเฉพาะฟิลด์หลักที่ยังบังคับตามกฎเหล็กเดิมใน CLAUDE.md (วันที่, ทีมงาน, แบรนด์ — "งานในแพลนงานรายวันทุกชิ้นต้องผูกกับแบรนด์เสมอ", จำนวนชิ้น)
1) เอาเครื่องหมาย * และการเช็คบังคับของ Product ออกทั้งหมด (ทุกประเภทงาน ไม่ใช่แค่ตัดคลิป) — label เปลี่ยนเป็น "(ไม่บังคับ)" นิ่งตลอด ดรอปดาวน์มีตัวเลือก "— ไม่ระบุ —" เสมอไม่ว่าประเภทงานไหน
2) เอาโค้ดที่แยกพฤติกรรมตามประเภทงาน (isClipWorkType / updateClipFieldRequirement) ออก เพราะไม่มีฟิลด์ไหนผูกกับประเภทงานอีกแล้ว — Format Type/Content Pillar/Product เป็นแค่ตัวเลือกเสริมเท่ากันหมดไม่ว่าจะเลือกประเภทงานอะไร
3) ฟิลด์ที่ยังบังคับอยู่ (ห้ามเอาออก เพราะขัดกฎเหล็ก CLAUDE.md): วันที่, ทีมงาน, แบรนด์, จำนวนชิ้น
เช็คพอยต์: เลือกประเภทงานอะไรก็ได้ เว้นว่าง Product/Image/ลิงก์ไดรฟ์งานทั้งหมด กรอกแค่วันที่/ทีมงาน/แบรนด์/ประเภทงาน/จำนวนชิ้น บันทึกได้สำเร็จ → ลองเว้นว่างแบรนด์ ยังต้องถูกบล็อก (ไม่ได้เปลี่ยน เพราะเป็นกฎเหล็ก)

## TASK 29 — กดดูสรุปงานรายคนได้จากหน้าแดชบอร์ด
เดิม "แดชบอร์ดรายคน" (สรุปผลงานละเอียดต่อคน) เข้าถึงได้จากปุ่ม "📊 แดชบอร์ด" ที่หน้าทีมงานเท่านั้น — เพิ่มทางเข้าจากหน้าแดชบอร์ดหลักด้วย เพราะหัวหน้าทีมดูภาพรวมที่หน้านี้อยู่แล้ว ไม่อยากต้องสลับไปหน้าทีมงานก่อน
1) การ์ด "ชิ้นงานแยกตามคน (เดือนนี้)" ที่หน้าแดชบอร์ด (bar list รายชื่อ+แท่งกราฟ) คลิกที่แถวคนไหนได้เลย เปิดแดชบอร์ดรายคนของคนนั้นทันที
2) ปุ่ม "← กลับ" ในหน้าแดชบอร์ดรายคน ต้องพากลับไปหน้าที่เพิ่งมาจากจริงๆ (ถ้ามาจากแดชบอร์ดหลัก กลับไปแดชบอร์ดหลัก ข้อความปุ่มเปลี่ยนเป็น "กลับไปแดชบอร์ด" — ถ้ามาจากหน้าทีมงานเหมือนเดิม กลับไปหน้าทีมงาน ข้อความเดิม "กลับไปหน้าทีมงาน")
เช็คพอยต์: หน้าแดชบอร์ดหลัก คลิกแถวชื่อคนในการ์ด "ชิ้นงานแยกตามคน" → เปิดแดชบอร์ดรายคนของคนนั้นถูกต้อง → กดปุ่มกลับ ต้องพากลับมาหน้าแดชบอร์ดหลัก (ไม่ใช่หน้าทีมงาน) → ไปหน้าทีมงาน กดปุ่ม "📊 แดชบอร์ด" ต่อแถวเหมือนเดิม กดกลับต้องพากลับไปหน้าทีมงานเหมือนเดิม (ไม่ได้เปลี่ยนพฤติกรรมเดิม)

## TASK 30 — ตรวจ+ปรับหน้าตาให้ใช้มือถือง่ายขึ้น + ทบทวนความมินิมอล
ผู้ใช้ขอให้ปรับหน้าตาแอพให้เข้ากับการใช้ในมือ และขอสไตล์ "ดูง่าย มินิมอล" — เช็คแล้วพบว่า Design System "HomeOffice" ใน CLAUDE.md ล็อกฟอนต์ (Sarabun/IBM Plex Sans Thai) สี และการ์ด/แท็บ active ไว้ชัดเจนอยู่แล้วโดยตั้งใจให้ดูมินิมอล/มืออาชีพ (ไม่ใช่ดีไซน์ทั่วไปที่ต้องเปลี่ยน) — ถามผู้ใช้แล้วว่ายืนยันคงฟอนต์/สีเดิมตาม CLAUDE.md ไม่เปลี่ยน ไม่ทับกฎที่ล็อกไว้เงียบๆ
สิ่งที่ทำจริง (ไม่แตะสี/ฟอนต์ที่ล็อกไว้):
1) พบบั๊กจริงตอนไล่เช็คมือถือ: ปุ่ม "🔄 รีเฟรชข้อมูล" ที่เพิ่มใน TASK 25 ตัวหนังสือยาวเกินจนล้นตัวปุ่ม/ดันความสูง topbar บนจอมือถือ — แก้โดยซ่อนข้อความ "รีเฟรชข้อมูล" และ "คอนเทนต์ใกล้ถึงวันลง" บนจอแคบ เหลือแค่ไอคอน/ตัวเลข เต็มข้อความกลับมาบนจอกว้าง
2) เพิ่ม `white-space:nowrap` ให้ `.icon-btn` ทั้งระบบกันปัญหาแบบเดียวกันเกิดซ้ำจุดอื่น
3) ไล่ตรวจหน้าล็อกอิน/แดชบอร์ด/ฟอร์มบันทึกงาน/เมนู ☰ บนจอมือถือจริง (375px) ครบ — เค้าโครงเดิม (การ์ดขาว ฟอร์มคอลัมน์เดียว ปุ่มใหญ่พอนิ้วโป้ง) ใช้งานได้ดีอยู่แล้วตามที่ CLAUDE.md วางไว้ ไม่ต้องปรับเพิ่ม
เช็คพอยต์: เปิดแอปที่ความกว้างจอ 375px → topbar ไม่ล้น ปุ่มรีเฟรชเป็นวงกลมไอคอนเดียว badge แจ้งเตือนไม่ขึ้นสองบรรทัด → ขยายจอเป็นเดสก์ท็อป ข้อความเต็มกลับมาเหมือนเดิม

## TASK 31 — ย่อฟอร์มบันทึกงาน + บันทึกหลายประเภทงานในครั้งเดียว + Product ค้นหาได้ + แดชบอร์ด/ทีมงานอัดแน่นพอแคปจอเดียว
ผู้ใช้ขอ 4 เรื่องรวดในคำขอเดียว: (1) เอา Format Type/Content Pillar ออกจากฟอร์มบันทึกงานทั้งหมด เหลือแต่ฟิลด์สำคัญ (2) ฟอร์มเพิ่มงานเร็วหน้ารายงานการทำงาน บันทึกครั้งเดียวติ๊กได้หลายประเภทงาน (เช่น ทำทั้งตัดคลิป+ออกกอง+ถ่ายฟุตเทจในวันเดียว บันทึกทีเดียวตอนเย็นแทนที่ต้องกรอกทีละประเภท) (3) หน้าแดชบอร์ดทั้งภาพรวมและรายคน ให้ดูง่ายพอแคปหน้าจอเดียวเห็นครบ เจ้านายเข้าใจทันทีว่าใครทำอะไรบ้าง ฟอนต์เล็กลงได้ (4) หน้าทีมงานยังใช้ยากบนมือถือ (ตารางกว้างเกิน)
1) ลบ Format Type/Content Pillar ออกทั้งระบบ — ฟิลด์ในฟอร์ม (ฟอร์มเร็ว+ฟอร์มแก้ไข), ส่วนจัดการรายการที่หน้าแบรนด์, ฟังก์ชัน JS ทั้งหมด (renderFormatTypeList/addFormatType/renameFormatType/deleteFormatType และคู่ Content Pillar), event listener, การแสดงผลในตารางแพลนงานรายวัน, คีย์ `formatTypes`/`contentPillars` ใน seedData/migrateData — ค่าที่เคยบันทึกไว้ในงานเก่า (`w.formatType`/`w.contentPillar`) ไม่ได้ลบทิ้งจากข้อมูลจริง แค่ไม่มี UI เก็บ/แสดงต่อแล้ว (กันแก้ข้อมูล production เดิมโดยไม่จำเป็น)
2) ฟอร์ม "บันทึกงานวันนี้" (หน้ารายงานการทำงานประจำวัน) ออกแบบใหม่ทั้งหมด — เหลือฟิลด์ร่วม: วันที่/ทีมงาน/แบรนด์/Product/รายละเอียด (ใช้ร่วมกันทุกประเภทที่ติ๊ก) + ตารางติ๊กประเภทงาน (`WORK_TYPES` ทั้ง 6 ประเภท) แต่ละแถวมีช่องกรอกจำนวนชิ้นของตัวเอง ติ๊กได้พร้อมกันหลายประเภท กดบันทึกครั้งเดียว ระบบสร้าง workItem+dailyReport แยกกันหนึ่งคู่ต่อหนึ่งประเภทที่ติ๊ก (ไม่ได้เปลี่ยนโครงสร้างข้อมูล แค่ให้ 1 ฟอร์ม submit ได้หลายก้อนงานพร้อมกัน) — ตัดฟิลด์สถานะ/Image/ลิงก์ไดรฟ์ออกจากฟอร์มเร็วนี้ไปเลย (ใช้ค่าเริ่มต้น แก้ทีหลังได้จากตารางแพลนงานรายวัน/ฟอร์มแก้ไข)
3) ดรอปดาวน์ Product (ทั้งฟอร์มเร็วและฟอร์มแก้ไขงาน) เปลี่ยนจาก `<select>` ธรรมดาเป็นช่องค้นหา+รายการกรองสด (พิมพ์คำค้น กรองสินค้าของแบรนด์ที่เลือกอยู่แบบ real-time ไม่ใช้ไลบรารีภายนอก) จำเป็นเพราะบางแบรนด์มีสินค้าเกิน 40 รายการ เลื่อนหา dropdown ธรรมดายากบนมือถือ
4) หน้าแดชบอร์ดหลัก: รวมแถบ KPI 6 ตัว (ชิ้นงานเดือนนี้/ลงแล้ว/ส่งรีพอร์ต/กำลังทำ/รอตรวจ/โพสต์แล้ว) เป็นแถบเดียว, การ์ดสรุปแบรนด์เปลี่ยนจากรายการเรียงยาวเป็นกริดการ์ดย่อ 2 คอลัมน์บนมือถือ, ตาราง "แยกตามประเภทงาน" เปลี่ยนเป็นชิปคำเดียวเรียงแถวแทนตาราง 6 แถว, การ์ด "ชิ้นงานแยกตามคน" ปรับหัวข้อเป็น "ใครทำอะไรบ้าง (เดือนนี้)" ให้ตรงจุดประสงค์ที่เจ้านายอยากเห็น — หน้าแดชบอร์ดรายคนก็ปรับแบบเดียวกัน (รวมการ์ดชื่อ+ข้อมูลพนักงานเป็นการ์ดเดียว, KPI 3 ตัวเป็นแถบเดียว, ตารางแยกประเภทงานเป็นชิปแถว)
5) หน้าทีมงาน: ตารางสมาชิกใส่คลาส `responsive-collapse` — จอกว้างยังเป็นตารางปกติ จอแคบ (≤860px) พับแต่ละแถวเป็นการ์ดคอลัมน์เดียว (label ชิดซ้าย ค่าชิดขวา) ใช้กลไก CSS `data-label` ล้วน ไม่ต้องมี JS แยกสองชุด — ใช้คลาสเดียวกันกับตาราง "รีพอร์ตล่าสุด" ในหน้าแดชบอร์ดรายคนด้วย
เช็คพอยต์: หน้ารายงานการทำงาน เปิดฟอร์มบันทึกงาน ติ๊ก "ตัดคลิป" (ใส่จำนวน 1) และ "ออกกอง" (ใส่จำนวน 3) พร้อมกัน กดบันทึกครั้งเดียว → ตารางแพลนงานรายวันต้องมีงานใหม่ 2 รายการแยกกัน (ตัดคลิป 1 ชิ้น, ออกกอง 3 ชิ้น) ทั้งคู่ผูกแบรนด์/คนเดียวกันตามที่กรอก → พิมพ์ค้นหาในช่อง Product เห็นรายการกรองตรงคำค้นทันที เลือกแล้วช่องแสดงชื่อสินค้าที่เลือกถูกต้อง → เปิดหน้าแดชบอร์ดที่จอกว้าง 375px แคปหน้าจอ ต้องเห็น KPI ครบ+สรุปแบรนด์+ใครทำอะไรบ้าง โดยเลื่อนดูไม่เกิน 1-2 หน้าจอ → หน้าทีมงานที่จอ 375px แต่ละคนต้องเป็นการ์ดอ่านง่าย ไม่ใช่ตารางเลื่อนซ้ายขวา
ทดสอบด้วยการรันแอปผ่าน `python3 -m http.server` แล้วขับเคลื่อนผ่าน browser devtools โดยตรง (จำลองการติ๊ก/บันทึกด้วย `saveData`/`logActivity` ที่ mock ไว้ชั่วคราว ไม่ให้กระทบข้อมูลจริงบน Supabase) ก่อนปล่อยของจริง — ตรวจสอบ payload บน Supabase ก่อน-หลังด้วย curl ยืนยันว่าจำนวน workItems/dailyReports/brands/members ไม่เปลี่ยนจากการทดสอบ

## TASK 32 — เปลี่ยนฟอร์มบันทึกงานเร็วเป็น "เพิ่มก้อนงานได้เรื่อยๆ ฟิลด์เปลี่ยนตามประเภทงาน" + ไล่แก้บั๊กมือถือทั้งระบบ + ย่อการ์ดทีมงานมือถือเพิ่ม
ต่อจาก TASK 31 — ผู้ใช้ทดลองใช้ฟอร์มติ๊กหลายประเภทของ TASK 31 แล้วขอเปลี่ยนแนวทางใหม่ทั้งหมด เพราะแต่ละประเภทงานจริงๆ ต้องการฟิลด์ไม่เหมือนกัน (ตัดคลิปต้องมีสถานะ/รูป/ไดร์ฟ แต่ออกกองอยากมีสถานที่แทน) และพบว่ายังมีบั๊กมือถือหลงเหลืออยู่หลายหน้า รวมถึงหน้าทีมงานที่ปรับใน TASK 31 ยังรู้สึกว่าเยอะเกินไปบนมือถือ
1) รื้อฟอร์ม "บันทึกงานวันนี้" ใหม่ทั้งหมด เป็นรายการ "ก้อนงาน" (`.aw-block`) ที่กดปุ่ม "+ เพิ่มงาน" เพิ่มได้เรื่อยๆ — แต่ละก้อนมีวันที่ (พรีฟิลจากก้อนก่อนหน้าอัตโนมัติ) + ตัวเลือกประเภทงาน แล้วฟิลด์ที่เหลือ regenerate ตามประเภทงานที่เลือกในก้อนนั้นทันที (onchange รีเฟรชแค่ container ของก้อนนั้น ไม่กระทบก้อนอื่น):
   - ตัดคลิป: แบรนด์*, รายละเอียด, จำนวน*, สถานะ, รูป* (บังคับเสมอ), ไดร์ฟงาน (บังคับเมื่อสถานะ=รอตรวจ/สำเร็จ — label สลับอัตโนมัติ), ลิงค์โพสต์ (โผล่+บังคับเมื่อสถานะ=Post เท่านั้น)
   - ออกกอง: แบรนด์*, สถานที่ (ฟิลด์ใหม่ `location` เพิ่มใน workItems schema), รายละเอียด
   - พากย์เสียง: แบรนด์*, จำนวน*
   - ถ่ายฟุตเทจ/เขียนสคริปต์/อื่นๆ: แบรนด์*, รายละเอียด (fallback เดียวกัน)
   - ทีมงาน (ownerId) ไม่ต้องเลือกจากดรอปดาวน์อีกต่อไป — ใช้ผู้ที่ล็อกอินอยู่ตอนนั้นเสมอ (`getCurrentUserId()`) — ฟอร์มแก้ไขงานเต็ม (ตาราง/ปฏิทิน) ยังเลือกทีมงานได้ตามเดิมสำหรับกรณีมอบหมาย/แก้ไขให้คนอื่น
   - ตัดฟิลด์ Product ออกจากฟอร์มนี้ (ยังมีในฟอร์มแก้ไขงานเต็ม) — ประเภทที่ไม่มีช่องจำนวนแสดง บันทึกจำนวน = 1 อัตโนมัติ — แบรนด์ยังบังคับทุกประเภทงาน (รวมถึงประเภทที่ผู้ใช้ระบุ field list มาไม่มีแบรนด์ด้วย) เพื่อรักษากฎเหล็ก CLAUDE.md "งานทุกชิ้นต้องผูกกับแบรนด์เสมอ" ไว้ — ถามผู้ใช้เรื่องนี้ก่อนแล้วไม่ได้คำตอบ จึงใช้ทางเลือกที่ปลอดภัยกว่า (คงกฎเดิม)
   - ปุ่ม "ลบ" ต่อก้อน (เหลือก้อนสุดท้ายลบไม่ได้ ระบบเติมก้อนว่างใหม่แทน) + ปุ่ม "💾 บันทึกทั้งหมด" ตรวจสอบทุกก้อนแล้วสร้าง workItem+dailyReport แยกคู่ต่อก้อน (คนละแบรนด์/ประเภทงานพร้อมกันได้) บันทึกสำเร็จรีเซ็ตกลับเหลือก้อนว่าง 1 ก้อน
   - เพิ่ม field ใหม่ "สถานที่" (`location`) ใน workItems schema + ฟอร์มแก้ไขงานเต็ม (ตาราง/ปฏิทิน) ก็แก้ไขได้ด้วย + โชว์ในตารางแพลนงานรายวัน
2) ไล่ตรวจทุกหน้าที่จอ 375px จริง เจอบั๊กและแก้: ตารางแพลนงานรายวัน/แพลนคอนเทนต์/รีพอร์ตรายวัน/สรุปรีพอร์ตรายเดือน(คอลัมน์ไดนามิก)/ประวัติการทำงาน ล้นขอบจอขวาไม่มี scroll เพราะยังไม่ได้ใส่คลาส `responsive-collapse`/`data-label` ตั้งแต่ TASK 31 (ตอนนั้นทำแค่ตารางทีมงานกับรีพอร์ตล่าสุดในแดชบอร์ดรายคน) — ใส่คลาส+attribute ให้ครบทุกตารางที่มีความเสี่ยง (ตารางประวัติการเข้าใช้งานแค่ 2 คอลัมน์สั้นๆ ไม่ต้องใส่เพราะไม่ล้น) และเจอบั๊กปฏิทิน: `.calendar-grid`/`.calendar-weekday-row` ใช้ `grid-template-columns:repeat(7,1fr)` ซึ่ง CSS Grid ไม่บีบคอลัมน์ให้เล็กกว่าเนื้อหาข้างในอัตโนมัติ (ชื่อคอนเทนต์ยาวใน `.calendar-item` ดันให้ทั้งตารางกว้างเกินจอ ล้นขวาโดยไม่มี ellipsis) แก้โดยเปลี่ยนเป็น `repeat(7,minmax(0,1fr))` และเพิ่ม `min-width:0` ที่ `.calendar-cell`
3) หน้าทีมงานบนมือถือ ปรับอีกรอบ (ผู้ใช้บอกว่ายังเยอะไปแม้ปรับใน TASK 31 แล้ว) — เลิกใช้กลไก `responsive-collapse` ทั่วไปสำหรับตารางนี้ เปลี่ยนเป็นการ์ดสรุปย่อเฉพาะ (`.team-mcard`) ที่ออกแบบมาให้แน่นกว่า: แถวบนสุด avatar+ชื่อ+ตำแหน่ง·แผนก(บรรทัดเดียว)+dropdown สถานะ (อยู่แถวเดียวกันหมด), แถวถัดมา "เข้าร่วมวันที่" คู่กับ "ชิ้นงานเดือนนี้" (แถวเดียว), ปิดท้ายปุ่มแดชบอร์ด/แก้ไข — ตัดฟิลด์ "แผนก" และ "รหัสพนักงาน" ออกจากมุมมองมือถือ (ยังดูได้เต็มจากปุ่ม "แดชบอร์ด"/"แก้ไข") ลดจาก ~7 แถวแยกเหลือ ~3 แถวต่อคน ย่อความสูงการ์ดลงกว่าครึ่ง — จอกว้าง (>860px) ยังเป็นตารางเดิมทุกอย่างไม่กระทบ
เช็คพอยต์: หน้ารายงานการทำงาน กด "+ เพิ่มงาน" 2 ครั้ง ได้ 3 ก้อน เปลี่ยนก้อนที่ 2 เป็น "ออกกอง" เห็นช่อง "สถานที่" แทนช่องเดิม → เลือกสถานะ "รอตรวจ" ในก้อนตัดคลิป เห็น label ไดร์ฟงานเปลี่ยนเป็นมีดอกจัน → เปลี่ยนเป็น "Post" เห็นช่องลิงค์โพสต์โผล่ขึ้นมาบังคับกรอก → ลบก้อนกลางออก ก้อนที่เหลือเลขลำดับต้องปรับใหม่ถูกต้อง → กด "บันทึกทั้งหมด" ได้ workItem+dailyReport ตามจำนวนก้อนที่กรอกครบ → เปิดทุกหน้าที่จอ 375px ไล่ดูตารางทุกหน้าต้องไม่มีคอลัมน์ล้นขอบขวา ปฏิทินต้องเห็นครบ 7 วันในจอเดียวไม่ล้น → หน้าทีมงานที่จอ 375px แต่ละคนต้องเหลือแค่ ~3 แถวข้อมูล ไม่ใช่รายการยาวเหมือนก่อนหน้า
ทดสอบด้วย mock `saveData`/`logActivity` ชั่วคราวเหมือน TASK 31 ไม่ให้กระทบข้อมูลจริง ตรวจ Supabase ก่อน-หลังด้วย curl ยืนยันจำนวน workItems/dailyReports ไม่เปลี่ยนจากการทดสอบ

## TASK 33 — ปฏิทินเปลี่ยนบทบาทเป็น "บอกเหตุการณ์" แบบ Google Calendar + แก้บั๊กวันที่ไม่ตรงเวลาไทยตอนกลางคืน
ผู้ใช้อยากให้หน้าปฏิทิน (1) ซิงค์กับวันที่จริง (2) มีวันสำคัญ/วันนักขัตฤกษ์ (3) โชว์เฉพาะเหตุการณ์ที่เพิ่มเองเหมือน Google Calendar ไม่ต้องโชว์งานที่ทีมงานบันทึกรายวัน (4) ใครก็เพิ่มบันทึกได้ — ตรวจโค้ดพบสาเหตุจริงของข้อ (1): `todayISO()` เดิมใช้ `new Date().toISOString().slice(0,10)` ซึ่งแปลงเวลาเป็น UTC ก่อนตัดวันที่ ทำให้ช่วงเที่ยงคืน-ตี 7 ตามเวลาไทย (เร็วกว่า UTC 7 ชั่วโมง) ระบบเข้าใจผิดว่ายังเป็น "เมื่อวาน" อยู่ — กระทบทั้งไฮไลต์วันนี้ในปฏิทิน, ค่าเริ่มต้นวันที่ในฟอร์มบันทึกงาน, และเดือนปัจจุบันของแดชบอร์ด
1) แก้ `todayISO()` ให้คำนวณจาก `getFullYear()`/`getMonth()`/`getDate()` ของเวลาท้องถิ่นเบราว์เซอร์แทน `toISOString()` — ใช้ได้ถูกต้องไม่ว่าเปิดแอปเวลาไหนของวันตามเวลาไทย
2) เพิ่มรายการวันสำคัญ/วันนักขัตฤกษ์ไทยแบบวันที่ตายตัว (`THAI_FIXED_HOLIDAYS` — วันขึ้นปีใหม่, วาเลนไทน์, จักรี, สงกรานต์ 3 วัน, แรงงาน, ฉัตรมงคล, เฉลิมพระชนมพรรษาราชินี/รัชกาลที่ 10, แม่แห่งชาติ, คล้ายวันสวรรคต ร.9, ปิยมหาราช, ฮาโลวีน, พ่อแห่งชาติ, รัฐธรรมนูญ, คริสต์มาส, สิ้นปี) <เดาให้ — แก้ได้ครบทุกรายการ> — ไม่รวมวันหยุดจันทรคติ (มาฆบูชา/วิสาขบูชา ฯลฯ) เพราะวันเปลี่ยนทุกปีตามปฏิทินจันทรคติ ไม่ควร hardcode ผิดๆ — แสดงเป็นตัวอักษรแดงเล็กใต้เลขวันที่ในช่องปฏิทิน
3) เพิ่มข้อมูลชนิดใหม่ `calendarEvents` (id, date, title, note, createdBy, createdAt) — ใครล็อกอินอยู่ก็เพิ่ม/แก้ไข/ลบได้ผ่านปุ่ม "+ เพิ่มเหตุการณ์" หรือคลิกวันที่ว่างในตาราง เปิด modal ใหม่กรอกวันที่/ชื่อเหตุการณ์ (บังคับ)/รายละเอียด (ไม่บังคับ) — ไม่ผูกกับแบรนด์/สถานะ ไม่กระทบตัวเลขแดชบอร์ด/สรุปรีพอร์ตใดๆ
4) เอาการแสดงผล workItems ออกจากปฏิทินทั้งหมด (ทั้งชิปงานและปุ่ม/คลิกเพื่อเพิ่มงาน) เปลี่ยนปุ่มหัวปฏิทินจาก "➕ เพิ่มงาน" เป็น "➕ เพิ่มเหตุการณ์" และคลิกวันที่ว่างเปิดฟอร์มเพิ่มเหตุการณ์แทนฟอร์มเพิ่มงาน — ชิปคอนเทนต์ (จากแพลนคอนเทนต์) ยังโชว์เหมือนเดิม แต่ตอนนี้คลิกได้เพื่อเปิดฟอร์มแก้ไขคอนเทนต์นั้นโดยตรง (เดิมคลิกแล้วไม่มีผล จะไปทริกเกอร์ปุ่มเพิ่มงานของทั้งช่องแทน)
เช็คพอยต์: เปิดแอปช่วงเวลาใดก็ได้ วันที่ไฮไลต์ในปฏิทินต้องตรงกับวันที่จริงตามเวลาเครื่อง → เดือนที่มีวันสำคัญ (เช่น 12 ส.ค. วันแม่) ต้องเห็นป้ายสีแดงใต้เลขวันที่ → คลิกวันที่ว่าง เปิดฟอร์มเพิ่มเหตุการณ์พรีฟิลวันนั้น กรอกชื่อแล้วบันทึก เห็นชิปสีม่วงขึ้นในวันนั้นทันที → คลิกชิปเหตุการณ์ที่เพิ่งสร้าง เปิดฟอร์มแก้ไข ลบได้ → ปฏิทินต้องไม่มีชิปงาน "📋" จาก workItems ให้เห็นอีกต่อไป (แม้จะมี workItem บันทึกไว้ในวันนั้นก็ตาม) → คลิกชิปคอนเทนต์จากแพลนคอนเทนต์ เปิดฟอร์มแก้ไขคอนเทนต์นั้นถูกต้อง
ทดสอบด้วย mock `saveData`/`logActivity` ชั่วคราว ไม่ให้กระทบข้อมูลจริงบน Supabase ระหว่างทดสอบ CRUD ของ calendarEvents ตรวจ Supabase ก่อน-หลังด้วย curl (เทียบ `updated_at` ต้องไม่ขยับถ้าไม่ได้ตั้งใจบันทึกจริง)

## TASK 34 — ไล่ตรวจ edge case ทั้งระบบ (/edgecase) ก่อนใช้จริง
ผู้ใช้ขอให้ตรวจสอบก่อน — ไล่เช็คคลังกับดักที่เจอบ่อยในระบบใช้งานจริง (วันที่, กดซ้ำ, ค่าว่าง, อ้างอิงค้าง, หน้าจอ) กับโค้ดจริง เจอ 4 จุดที่เป็นบั๊กจริง แก้ให้เลยทุกจุด — เงิน/เอกสารภาษี ข้ามเพราะแอปนี้ไม่มีเรื่องเงิน
1) `toISOString().slice(0,10)` อีกจุดที่หลงเหลือ (ใน `migrateWorkItems()` ตอน fallback วันที่ของ workItem เก่าที่ไม่มี date) — เปลี่ยนไปใช้ `todayISO()` ที่แก้ไปแล้วใน TASK 33 ให้สอดคล้องกันทั้งระบบ
2) เพิ่มกลไกกันกดปุ่มบันทึกซ้ำ (`guardClick`) — เดิมไม่มีเลยทั้งแอป กดรัวได้ข้อมูลซ้ำ ใส่ครบทุกปุ่มบันทึกหลัก 11 ปุ่ม (เพิ่มงาน/แก้ไขงาน/แบรนด์/สมาชิก/คอนเทนต์/สคริปต์/ไอเดีย/เหตุการณ์ปฏิทิน/ตั้งค่าแบรนด์/ข้อมูลโพสต์/เพิ่มสมาชิกด่วน) — ปิดปุ่มทันทีที่กด เปิดใหม่หลัง 700ms
3) `deletePlan()` ลบคอนเทนต์ในแพลนคอนเทนต์ได้แม้มีสคริปต์ผูกอยู่ (ทำให้สคริปต์ลอยไม่มีคอนเทนต์อ้างอิง ขัดกฎเหล็กของระบบเอง) — แก้ให้บล็อกเหมือนกับที่ `deleteBrand()`/`deleteMemberHard()` ทำอยู่แล้ว พร้อมข้อความบอกให้ไปจัดการสคริปต์ก่อน
4) เพิ่มปุ่ม Esc ปิด modal ได้ทุกฟอร์มป๊อปอัพในระบบ (เดิมมีแค่คลิกฉากหลังปิด) — ใช้ตัวจับ keydown ตัวเดียวที่ document หาค่า `.modal-overlay.open` แล้วปิดให้ ไม่ต้องแก้ทีละ modal
เช็คพอยต์: กดปุ่มบันทึกงานรัว 3 ครั้งติดกัน ต้องได้แค่ 1 รายการ ไม่ใช่ 3 → ลองลบคอนเทนต์ที่มีสคริปต์เขียนผูกไว้แล้ว ต้องถูกบล็อกพร้อมข้อความชัดเจน → เปิด modal ใดก็ได้ กด Esc ต้องปิดทันที → ตรวจ Supabase ก่อน-หลังด้วย curl ยืนยัน `updated_at`/จำนวนแถวไม่เปลี่ยนจากการทดสอบ

## TASK 35 — หน้าสรุปรีพอร์ต (รายเดือน) ปรับเป็นสรุป KPI
ผู้ใช้อยากให้หน้าสรุปรีพอร์ตดูเป็น KPI มากขึ้น (เดิมมีแต่ตารางตัวเลขคน×ประเภทงานล้วน) — ถามแล้วเลือก 3 ตัวชี้วัดหลัก: เทียบเดือนก่อนหน้า, อันดับคนทำงานเยอะ, เฉลี่ยงาน/วันทำงาน (ไม่เอาอัตราสำเร็จงาน) และยืนยันเก็บตารางละเอียดเดิมไว้ด้านล่าง KPI ไม่ตัดออก
1) เพิ่มการ์ด KPI 3 ใบเหนือตารางเดิมในโหมด "รายเดือน": (ก) ยอดชิ้นงานเดือนนี้ พร้อม badge เทียบเดือนก่อนหน้า (↑X% สีเขียว / ↓X% สีแดง / "เท่าเดือนก่อน" สีเทา / "ใหม่ทั้งหมด" ถ้าเดือนก่อนไม่มีข้อมูลเลย) — คำนวณจากผลรวม dailyReports ของเดือนก่อนหน้าจริง ไม่ใช่เลขสมมติ (ข) เฉลี่ยชิ้นงาน/วันทำงาน = ยอดรวมเดือนนี้ ÷ จำนวนวันที่มี dailyReports อย่างน้อย 1 รายการในเดือนนั้นจริง (ค) คนทำงานเยอะสุด (ชื่อ+จำนวนชิ้น)
2) เพิ่มการ์ด "อันดับคนทำงาน (เดือนนี้)" แสดงทุกคนที่มีผลงาน>0 เรียงมาก→น้อย พร้อมเลขอันดับ (อันดับ 1 มีป้ายสีทองเด่น) แบบกราฟแท่งเหมือนหน้าแดชบอร์ด — คลิกแถวเปิดแดชบอร์ดรายคนของคนนั้นได้ทันที ปุ่มกลับต้องพากลับมาหน้าสรุปรีพอร์ต (เพิ่ม return-path ใหม่ "summary" ให้ `openMemberDashboard()` เดิมรองรับแค่ "team"/"dashboard")
3) ตารางคน×ประเภทงานแบบละเอียดเดิม + ปุ่มออกรายงาน CSV ยังอยู่เหมือนเดิมทุกอย่างด้านล่าง KPI ไม่ได้แตะ
เช็คพอยต์: เลือกเดือนที่มีข้อมูลทั้งเดือนนี้และเดือนก่อน ตัวเลข % เทียบต้องตรงกับที่คำนวณเอง → เดือนที่เดือนก่อนไม่มีข้อมูลเลย ต้องขึ้น "ใหม่ทั้งหมด" ไม่ใช่ error/NaN → คลิกแถวอันดับคนทำงาน เปิดแดชบอร์ดรายคนถูกคน กดปุ่มกลับต้องพากลับมาหน้าสรุปรีพอร์ต (ไม่ใช่หน้าทีมงาน) → เปลี่ยนเดือนที่ไม่มีรีพอร์ตเลย การ์ด KPI ต้องขึ้น 0 ทุกช่องไม่ error

## TASK 36 — เปลี่ยนชื่อเมนู "สรุปรีพอร์ต" เป็น "สรุป KPI"
ผู้ใช้ขอเปลี่ยนคำในหน้า/เมนูจาก "สรุปรีพอร์ต" เป็น "สรุป KPI" ให้ตรงกับที่ปรับเนื้อหาไปแล้วใน TASK 35 — เปลี่ยนป้ายชื่อทุกจุดที่อ้างอิงถึงเมนูนี้ตรงๆ (data-page="summary" เดิม ไม่เปลี่ยน แค่ป้ายชื่อ) ได้แก่ เมนูไซด์บาร์, หัวข้อ h2 ในหน้า, ชื่อหน้าที่ topbar (PAGE_TITLES), ข้อความอ้างอิงในหน้ารายงานการทำงานประจำวัน, ป้ายปุ่มกลับจากแดชบอร์ดรายคน, และชื่อไฟล์ CSV ที่ออกจากหน้านี้ — จุดที่ใช้คำว่า "รีพอร์ต"/"สรุปรีพอร์ต" แบบทั่วไป (ไม่ใช่ชื่อเมนู เช่น "ยอดรีพอร์ต" ที่หมายถึงข้อมูล dailyReports) ไม่แตะ เพราะยังถูกต้องตามความหมายเดิม
เช็คพอยต์: เมนูซ้ายต้องเห็น "📈 สรุป KPI" แทน "สรุปรีพอร์ต" → เปิดหน้านั้น หัวข้อ/topbar ต้องขึ้น "สรุป KPI" ตรงกัน → กดปุ่มกลับจากแดชบอร์ดรายคน (มาจากหน้านี้) ต้องขึ้น "← กลับไปสรุป KPI" → ออก CSV จากโหมดรายเดือน ชื่อไฟล์ต้องมี "สรุป-KPI" แทน "สรุปรีพอร์ต"