# ไฟล์ CSV ที่ export ออกจากแอป มีข้อมูลส่วนตัวจริงของทีมงาน (ชื่อ-สกุล/วันเกิด/อีเมล) ไม่ควรเก็บใน git
*.csv
# ไฟล์ชีตแผนคอนเทนต์ต้นฉบับ (มีข้อมูลภายในทีมจริง + ไฟล์ใหญ่) — นำเข้า Supabase แล้ว ไม่ต้องเก็บใน git
*.xlsx
# รูปโลโก้ต้นฉบับที่ใช้อัปโหลดเข้าระบบ — อยู่ใน Supabase แล้ว (base64 ในข้อมูลแบรนด์)
logos/
.DS_Store
# โฟลเดอร์ที่ก๊อป app.html/index.html มาไว้ deploy ขึ้น Cloudflare Pages (wrangler pages deploy public)
# เก็บเฉพาะไฟล์ที่ต้องขึ้นเว็บสาธารณะ ไม่ให้ CLAUDE.md/SPEC.md/TASKS.md/ไฟล์ทีมงานหลุดขึ้นเว็บไปด้วย
public/
.wrangler/
