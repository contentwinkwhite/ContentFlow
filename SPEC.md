# SPEC.md — ระบบ "ContentFlow"

## ระบบนี้คืออะไร
ระบบหลังบ้านสำหรับหัวหน้าทีม Content Creator (3-5 คน) ใช้แทนชีต — ให้ทุกคนบันทึกงานประจำวันแบบเดียวกัน
หัวหน้าเห็นสรุปผลงานรายเดือนเป็นตัวเลขทันทีโดยไม่ต้องนั่งไล่รวมเอง พร้อมช่วยแพลนคอนเทนต์รายเดือน
เก็บไอเดีย/เทรนด์ และบังคับให้สคริปต์ทุกชิ้นออกมาในฟอร์แมตเดียวกัน

## ไฟล์ของระบบ
- index.html = showcase โชว์ภาพรวม · app.html = แอพจริง

## เมนูในระบบ (11 เมนู)
- แดชบอร์ด — ภาพรวมเดือนนี้ (จำนวนชิ้นงานรวม, แยกตามคน, แยกตามแบรนด์, แยกตามประเภทงาน, คอนเทนต์ที่ใกล้ถึงวันลง) + สรุปงานทีมแบบรวมทั้งหมด และแยกตามแบรนด์ (แบรนด์ที่ทำ, ช่อง TikTok ที่ลงงาน, งานที่กำลังทำ, งานที่รอตรวจ, งานที่โพสต์แล้ว)
- รายงานการทำงานประจำวัน — ฟอร์มเดียวบันทึกงานผูกกับแบรนด์แบบละเอียด กดครั้งเดียวได้ทั้งยอดรีพอร์ตนับจำนวนชิ้นรายเดือนและงานละเอียดพร้อมกัน (รวม "รีพอร์ตแบบเร็ว" กับ "เพิ่มงาน" เดิมเป็นฟอร์มเดียวตั้งแต่ TASK 22) — การ "ดู/สรุป" รีพอร์ตย้อนหลังอยู่ที่เมนู "สรุป KPI" (โหมดรายวัน)
- ปฏิทิน — (ตั้งแต่ TASK 33 เปลี่ยนบทบาทใหม่) ปฏิทินบอกเหตุการณ์ล้วนๆ แบบ Google Calendar — โชว์คอนเทนต์ที่วางแผนลง (จากแพลนคอนเทนต์), วันสำคัญ/วันนักขัตฤกษ์ไทย, และ "เหตุการณ์" ที่ใครก็เพิ่มเองได้ (calendarEvents) — ไม่โชว์งานที่ทีมงานบันทึกรายวัน (workItems) อีกต่อไป เพราะเป็นข้อมูลปฏิบัติงานละเอียด ดูได้ที่เมนู "รายงานการทำงานประจำวัน" แทน เลื่อนดูเดือนก่อน/ถัดไปได้ คลิกวันที่ว่างเพื่อเพิ่มเหตุการณ์ คลิกชิปคอนเทนต์/เหตุการณ์ที่มีอยู่แล้วเพื่อแก้ไข
- งานที่บริษัทรับผิดชอบ — จัดการรายชื่อแบรนด์ที่ทีมรับทำ (เดิมอยู่เป็นการ์ดในหน้าแดชบอร์ด ย้ายมาเป็นหน้าแยก) ใช้เป็นตัวเลือกดรอปดาวน์ตอนลงรีพอร์ต/แพลนงานรายวัน ทุกจุด
- แพลนคอนเทนต์ — ปฏิทิน/ตารางคอนเทนต์รายวัน-รายเดือน พร้อมสถานะ (ภาพรวมระดับหัวข้อ แยกจากแพลนงานรายวันซึ่งเป็นระดับปฏิบัติงาน)
- แพลนหน้าช่อง (ใหม่) — เมนูใหม่แยกต่างหาก ตารางกริดรูปหน้าปกคลิปที่พร้อมลง (ตัดคลิปเสร็จแล้ว มีรูปหน้าปกแล้ว) เลือกแบรนด์+ช่องทาง+เดือนที่จะวางแผน แล้วลากจัดเรียงลำดับได้ เหมือนดูตารางกริดหน้าโปรไฟล์ TikTok ก่อนลงจริง — หัวหน้าคอนเทนต์ใช้ตัดสินใจว่าจะเอาคลิปไหน หน้าปกอะไรลงก่อน-หลัง วางแผนล่วงหน้าได้หลายเดือน
- คลังสคริปต์ — เขียน/ดูสคริปต์ตามเทมเพลตมาตรฐาน ผูกกับแพลนคอนเทนต์
- ไอเดีย & เทรนด์ — บันทึกไอเดียคอนเทนต์ เทรนด์ที่เจอ แคปชั่นสำเร็จรูป
- ทีมงาน — รายชื่อสมาชิก + ข้อมูลพนักงาน (ตำแหน่ง/แผนก/รหัสพนักงาน/วันเกิด/เบอร์โทร/อีเมล) + สถานะแก้ไขได้ทันทีจากตาราง + สรุปผลงานรายเดือนต่อคน
- สรุป KPI (เดิมชื่อ "สรุปรีพอร์ต" / "สรุปรายเดือน" — ตั้งแต่ TASK 36 เปลี่ยนชื่อเมนูอีกครั้ง) — (ตั้งแต่ TASK 34) โหมด "รายเดือน" เน้น KPI ก่อนตาราง: การ์ดสรุปยอดรวม+เทียบเดือนก่อน, เฉลี่ยชิ้นงาน/วันทำงาน, คนทำงานเยอะสุด + การ์ดอันดับคนทำงานทั้งทีม แล้วค่อยตามด้วยตารางละเอียดคน×ประเภทงานแบบเดิม หรือดูรีพอร์ตของวันใดวันหนึ่งแบบละเอียด (โหมด "รายวัน") สลับได้ในหน้าเดียว + ออกรายงาน CSV ทั้งสองโหมด (CSV ยังออกจากตารางละเอียดเดิม ไม่รวม KPI)
- ประวัติการใช้งาน — ประวัติการเข้าใช้งาน (login) + ประวัติการทำงาน (activity log) ของแต่ละคน + ออกรายงาน CSV แยกทั้งสองตาราง

<เดาให้ — แก้ได้> "แบรนด์" ในที่นี้หมายถึงลูกค้า/แบรนด์สินค้าที่ทีมรับทำคอนเทนต์ให้ (ทีมดูแลได้หลายแบรนด์ แต่ละแบรนด์มีช่อง TikTok ของตัวเอง) ถ้าความหมายจริงไม่ตรงนี้ให้แก้ตรงนี้ก่อน

## ระบบเข้าสู่ระบบ (ไม่ใช่ระบบความปลอดภัยจริงจัง)
แอพนี้ไม่มีเซิร์ฟเวอร์หลังบ้าน จึง "เข้าสู่ระบบ" ทำได้แค่ระบุตัวตนผู้ใช้งาน (เลือกชื่อตัวเองจากสมาชิกที่ทำงานอยู่) ไม่มีรหัสผ่าน ไม่ใช่การป้องกันการเข้าถึงจริง — มีไว้เพื่อรู้ว่าใครกำลังใช้งานอยู่ และบันทึก log เท่านั้น
- เปิดแอพครั้งแรกในแท็บนั้น (ยังไม่เคยเลือกชื่อ) ต้องเจอหน้าเข้าสู่ระบบก่อนเข้าเมนูอื่น
- เลือกชื่อแล้วสถานะการล็อกอิน "จำไว้ในแท็บนั้น" (sessionStorage) รีเฟรชหน้าไม่ต้องเลือกชื่อซ้ำ แต่ปิดแท็บ/เปิดแท็บใหม่ต้องเลือกชื่อใหม่
- มีปุ่ม "ออกจากระบบ" ให้สลับตัวตนได้ทันทีโดยไม่ต้องปิดแท็บ
- แต่ละแถวในรายชื่อหน้าเข้าสู่ระบบ โชว์ตำแหน่ง (position) ใต้ชื่อ คลิกแถวเพื่อล็อกอินอย่างเดียว (ไม่มีปุ่มแก้ไขติดรายแถวแล้ว) — รูป/ชื่อ/ตำแหน่งจัดกึ่งกลางแถว ไม่ชิดซ้าย
- ด้านล่างสุดของการ์ดล็อกอินมีปุ่มเดียว "จัดการโปรไฟล์ผู้ใช้" เปิด modal "จัดการทีมงาน" เดียวกับหน้าทีมงาน (เพิ่มสมาชิกใหม่พร้อมชื่อ/ตำแหน่ง/แผนก, แก้ไขโปรไฟล์เต็ม, ลบ) ได้ทันทีก่อนล็อกอิน ไม่ต้องเลือกชื่อเข้าใช้ก่อน

## ข้อมูลในระบบ (ตั้งแต่ TASK 25 เก็บกลางบน Supabase ตาราง `contentflow_data` แถวเดียว คอลัมน์ `payload` เป็น jsonb — โครงสร้างข้างในเหมือนตอนเก็บใน localStorage คีย์ "contentflow_data" เดิมทุกอย่าง ต่างแค่ที่เก็บ)

**members** (ทีมงาน)
- id, ชื่อเล่น, วันที่เข้าร่วมทีม, หมายเหตุ, photoDataUrl (รูปโปรไฟล์ที่อัปโหลดเอง เก็บเป็น base64 data URL, default null = ใช้ตัวอักษรแรกของชื่อแทน)
- ตำแหน่ง (position), แผนก (department), รหัสพนักงาน (employeeCode), วันเดือนปีเกิด (birthDate), เบอร์โทรติดต่อ (phone), อีเมล (email) — ทุกช่องนี้ไม่บังคับกรอก (ไม่บล็อกการเพิ่มสมาชิกถ้ายังไม่มีข้อมูล)
- สถานะ (status) — เลือกได้ 4 ค่า: "เทสงาน" / "ทดลองงาน" / "ประจำ" / "พ้นทีม" — แก้ไขได้ทันทีจากตารางในหน้าทีมงาน (dropdown ในแถว ไม่ต้องเปิดฟอร์ม)
  ทุกจุดในระบบที่เคยเช็ค "ทำงานอยู่หรือไม่" (ใครโผล่ในดรอปดาวน์เลือกคนตอนบันทึกงาน, นับจำนวนสมาชิกที่ทำงานอยู่) ให้ถือว่า "ทำงานอยู่" = สถานะไม่ใช่ "พ้นทีม" (เทสงาน/ทดลองงาน/ประจำ นับเป็นทำงานอยู่ทั้งหมด)

**dailyReports** (รีพอร์ตประจำวัน) — หัวใจของระบบ ตัวเลขสรุปทั้งหมดมาจากตรงนี้
- id, memberId (ใครส่ง), วันที่, ประเภทงาน (ตัดคลิป / ถ่ายฟุตเทจ / ออกกอง / พากย์เสียง / เขียนสคริปต์ / อื่นๆ)
- จำนวนชิ้น, เชื่อมโยงกับ contentPlanId (ถ้ามี — งานนี้ทำเพื่อคอนเทนต์ชิ้นไหน), brandId (ถ้ามี — งานนี้ทำให้แบรนด์ไหน ไม่บังคับเลือก), หมายเหตุ

**contentPlans** (แพลนคอนเทนต์)
- id, หัวข้อคอนเทนต์, แพลตฟอร์ม (TikTok / Reels / Shorts / อื่นๆ), วันที่วางแผนลง
- สถานะ (ไอเดีย / เขียนสคริปต์ / ถ่ายทำ / ตัดต่อ / พร้อมลง / ลงแล้ว), ผู้รับผิดชอบ (memberId), หมายเหตุ

**brands** (แบรนด์ที่ทีมดูแล)
- id, ชื่อแบรนด์, ช่อง TikTok (ชื่อ/ลิงก์ช่อง — ช่องหลักเดิม เก็บไว้เผื่ออ้างอิง), หมายเหตุ
- products (รายชื่อสินค้าของแบรนด์นี้ — array ข้อความ) จัดการ เพิ่ม/แก้ไข/ลบ ได้จากหน้า "งานที่บริษัทรับผิดชอบ" ตอนแก้ไขแบรนด์นั้นๆ ใช้เป็นตัวเลือก Product ในฟอร์มรายงานการทำงาน (กรองตามแบรนด์ที่เลือก)
- channels (ใหม่ — รายชื่อ "ช่องทาง" ที่ลงคลิปของแบรนด์นี้ เช่น "TikTok หลัก", "TikTok ปั่น 1", "Shopee" — array ข้อความ จัดการเพิ่ม/แก้ไข/ลบได้อิสระแบบเดียวกับ products ไม่จำกัดจำนวน ไม่บังคับต้องมี — แบรนด์ที่มีช่องเดียวจะมี channels แค่ 1 รายการหรือปล่อยว่างก็ได้) ใช้เป็นตัวเลือก "ช่องทางที่ลง" ตอนเปลี่ยนสถานะงานตัดคลิปเป็น "Post"

**workItems** (รายงานการทำงาน) — 1 แถว = 1 ชิ้นงาน ปรับโครงสร้างใหม่ให้ตรงกับฟอร์มจริงที่ทีมใช้
- id, title (หัวข้องาน — พิมพ์เองได้อิสระ ไม่บังคับ ใช้เป็นชื่อที่โชว์ในตาราง)
- date (วันที่ *บังคับ), ownerId (ทีมงาน — memberId *บังคับ), brandId (แบรนด์ — เลือกจาก brands, <เดาให้ — แก้ได้> ตั้งแต่รอบปรับฟอร์มบันทึกงานเร็วนี้ **ไม่บังคับแล้ว** เลือก "อื่นๆ"/ปล่อยว่างได้ถ้างานนั้นไม่ผูกกับแบรนด์ไหน — งานที่ brandId ว่างจะไม่ถูกนับในสรุปแยกตามแบรนด์ทุกจุด แต่ยังนับในยอดรวมทั้งหมด)
- product (สินค้า — เลือกจาก products ของแบรนด์ที่เลือกไว้เท่านั้น ผ่านช่องค้นหา+กรองสด ไม่บังคับ — ใช้ในฟอร์มแก้ไขงานเท่านั้น ตั้งแต่ TASK 32 ฟอร์มบันทึกงานเร็วไม่มีช่อง Product แล้ว), location (สถานที่ — ใหม่ตั้งแต่ TASK 32 สำหรับงานประเภท "ออกกอง" โดยเฉพาะ ไม่บังคับ), imageNote (รูปภาพ/ลิงก์รูป), driveLink (ลิงก์ไดรฟ์งาน — ลิงก์เดียวรวมทุกไฟล์ของงานนี้ แทนที่ลิงก์ Draft 1/2 แยกของโครงสร้างเดิม)
  — ฟิลด์บังคับจริงของ workItem จากฟอร์มแก้ไขงาน (ตาราง/ปฏิทิน) เหลือแค่ date/ownerId/quantity เท่านั้น (brandId/imageNote/driveLink/product ไม่บังคับ) — ฟอร์มบันทึกงานเร็ว "บันทึกงานวันนี้" มีกติกาบังคับเพิ่มเฉพาะประเภทงาน "ตัดคลิป" เท่านั้น ดูรายละเอียดที่หัวข้อ "รายงานการทำงานประจำวัน" ด้านล่าง
- channel (ช่องทางที่ลง — ใหม่ เลือกจาก channels ของแบรนด์ที่เลือกไว้ ไม่บังคับทั่วไป แต่ **บังคับ** เมื่องานประเภท "ตัดคลิป" เปลี่ยนสถานะเป็น "Post" — ต้องรู้ว่าคลิปนี้ลงช่องทางไหนไปแล้ว)
- coverImageDataUrl (รูปหน้าปกคลิป — ใหม่ เก็บเป็น base64 data URL แบบเดียวกับรูปโปรไฟล์/โลโก้แบรนด์ที่มีอยู่แล้ว ไม่บังคับกรอกตอนบันทึกงานทั่วไป แต่ต้องมีถึงจะถูกดึงไปแสดงในหน้า "แพลนหน้าช่อง" ได้ — เดิมมีแค่ imageNote เป็นข้อความ/ลิงก์ ตอนนี้เพิ่มรูปจริงแยกต่างหากสำหรับใช้วางแผนกริดหน้าช่องโดยเฉพาะ)
- <เดาให้ — แก้ได้> (ตั้งแต่ TASK 31) เอา formatType/contentPillar ออกจากฟอร์มบันทึกงานและหน้าจัดการแบรนด์ทั้งหมดแล้ว — งานเก่าที่เคยมีค่าสองฟิลด์นี้ยังเก็บไว้ในข้อมูลเดิม (ไม่ได้ลบทิ้ง) แค่ไม่มี UI ให้กรอก/แก้/แสดงต่อ ถือเป็นฟิลด์ที่เลิกใช้แล้ว
- description (รายละเอียดงาน ไม่บังคับ)
- status (ดูรายการด้านล่าง ไม่บังคับ default "ดำเนินการ")
- postDate (วันที่โพสต์จริง), linkPost (ลิงก์โพสต์ที่ลงแล้ว) — เติมอัตโนมัติเมื่อเปลี่ยนสถานะเป็น "Post" (ดูกติกาด้านล่าง)
- reportId (ไม่บังคับ) — ผูกกับ dailyReports.id ของรีพอร์ตยอดชิ้นงานที่สร้างพร้อมกันตอนบันทึกงานนี้ (ตั้งแต่ TASK 22 เป็นต้นไป ฟอร์มบันทึกงานเดียวสร้างทั้ง workItem และ dailyReport พร้อมกันเสมอ) งานเก่าก่อน TASK 22 อาจไม่มีค่านี้ (null) จนกว่าจะถูกแก้ไขครั้งแรก
- <เดาให้ — แก้ได้> (ตั้งแต่ TASK 32) ownerId จากฟอร์มบันทึกงานเร็วไม่ได้มาจากดรอปดาวน์เลือกคนอีกต่อไป — ใช้ผู้ใช้ที่ล็อกอินอยู่ขณะนั้นเสมอ (getCurrentUserId()) ส่วนฟอร์มแก้ไขงานเต็มรูปแบบ (ตาราง/ปฏิทิน) ยังเลือกทีมงานจากดรอปดาวน์ได้ตามเดิม สำหรับกรณีแก้ไข/มอบหมายงานให้คนอื่น

สถานะของ workItems (ไม่ได้เรียงเป็นขั้นบังคับ เปลี่ยนข้ามไปมาได้): ดำเนินการ / รอตรวจ / แก้ไข / สำเร็จ / Post / ยกเลิก
- เปลี่ยนสถานะเป็น "Post" จาก dropdown ในตาราง (ไม่ต้องเปิดฟอร์มเพิ่มงานใหม่) ต้องเปิดกล่องเล็กถามวันที่โพสต์ + ลิงก์โพสต์ทันที บันทึกเข้า postDate/linkPost ของงานนั้นเลย แก้ไขวันที่/ลิงก์ภายหลังได้จากปุ่มเดียวกันซ้ำ
- <เดาให้ — แก้ได้> workItems ที่บันทึกไว้ก่อนหน้านี้ (โครงสร้างเดิมที่มี deadline/draft1/draft2/finalClip) ถูก migrate อัตโนมัติตอนโหลดข้อมูล: deadline/startDate → date, finalClipLink/draft2Link/draft1Link/linkRef (อันแรกที่มีค่า) → driveLink, สถานะเดิมแมปเป็นสถานะใหม่ตามนี้: ไอเดีย/วางแผน และ กำลังทำ → ดำเนินการ, รอตรวจ Draft 1 และ รอตรวจ Final → รอตรวจ, แก้ไข Draft 2 → แก้ไข, พร้อมโพสต์ → สำเร็จ, โพสต์แล้ว → Post

**scripts** (คลังสคริปต์)
- id, contentPlanId (ผูกกับแพลนคอนเทนต์ไหน), หัวข้อ
- Hook (3 วิแรก), เนื้อหา, CTA (ปิดท้ายอย่างไร), ผู้เขียน (memberId), วันที่เขียน

**ideas** (ไอเดีย & เทรนด์)
- id, ประเภท (ไอเดียคอนเทนต์ / เทรนด์ / แคปชั่นสำเร็จรูป), เนื้อหา, วันที่บันทึก, ผู้บันทึก

**sessions** (ประวัติการเข้าใช้งาน)
- id, memberId (ใครล็อกอิน), loginAt (วันที่-เวลาที่เลือกชื่อเข้าใช้)

**activityLog** (ประวัติการทำงานของแต่ละคน)
- id, memberId (ใครทำ — คนที่ล็อกอินอยู่ตอนนั้น), timestamp (วันที่-เวลา), module (ชื่อโมดูลภาษาไทย เช่น "รีพอร์ตประจำวัน"), action (คำอธิบายสั้นๆ เช่น "เพิ่มรีพอร์ต: ตัดคลิป 3 ชิ้น")

**calendarEvents** (เหตุการณ์ในปฏิทิน — ใหม่ตั้งแต่ TASK 33)
- id, date (วันที่ *บังคับ), title (ชื่อเหตุการณ์ *บังคับ), note (รายละเอียด ไม่บังคับ)
- createdBy (memberId ของคนที่ล็อกอินอยู่ตอนเพิ่ม), createdAt (วันที่-เวลาที่เพิ่ม)
- ใครก็เพิ่ม/แก้ไข/ลบได้ ไม่ผูกกับแบรนด์หรือสถานะใดๆ (ไม่ใช่ workItem — เป็นแค่บันทึกเหตุการณ์อิสระบนปฏิทิน เช่น ประชุมทีม, ไลฟ์สด, เดดไลน์แคมเปญ) ไม่มีผลต่อการคำนวณตัวเลขในแดชบอร์ด/สรุป KPI ใดๆ ทั้งสิ้น

**feedPlanItems** (แผนเรียงลำดับกริดหน้าช่อง — สำหรับเมนู "แพลนหน้าช่อง")
- id, workItemId (**ไม่บังคับ** — ผูกกับ workItems.id ของคลิปตัดคลิปที่มี coverImageDataUrl แล้ว ถ้ามาจากงานจริงของทีม / เว้นว่างถ้าเป็นรูปที่หัวหน้าคอนเทนต์อัปโหลดเองในหน้านี้โดยตรง — ลบ workItem ต้นทางต้องลบ feedPlanItem ที่ผูกอยู่ไปด้วย กันอ้างอิงค้าง)
- coverImageDataUrl (ใช้เฉพาะตอน workItemId ว่าง — รูปที่อัปโหลดเองในหน้าแพลนหน้าช่อง ไม่ผูกกับงานจริงของทีม), caption (โน้ต/แคปชั่นสั้นๆ ไม่บังคับ ใช้เฉพาะรายการที่อัปโหลดเอง)
- brandId (แบรนด์ *บังคับ), channel (ช่องทาง *บังคับ — เลือกจาก channels ของแบรนด์นั้น) — ลบแบรนด์ต้องลบ feedPlanItems ที่ผูกอยู่ไปด้วย กันอ้างอิงค้าง
- plannedMonth (เดือนที่วางแผนจะลง รูปแบบ YYYY-MM *บังคับ — แยกจาก workItems.date ซึ่งเป็นวันที่ทำงานเสร็จจริง เพราะวางแผนล่วงหน้าข้ามเดือนได้)
- order (เลขลำดับตำแหน่งในกริด ภายในกลุ่มแบรนด์+ช่องทาง+เดือนเดียวกัน — ใช้จัดเรียง ปุ่มลูกศรขึ้น/ลงสลับตำแหน่งแล้วเลขอัปเดตใหม่ทั้งกลุ่ม)
- <เดาให้ — แก้ได้> 1 workItem ผูกกับ feedPlanItem ได้สูงสุด 1 รายการ (คลิปหนึ่งชิ้นวางแผนได้ช่องทาง+เดือนเดียว ไม่ซ้อนแผนหลายที่) — ถ้าจะย้ายไปช่องทาง/เดือนอื่น ให้แก้ไข record เดิม ไม่สร้างใหม่ซ้ำ

**settings** (ตั้งค่าแบรนด์ของระบบ — object เดี่ยว ไม่ใช่ array)
- appName (ชื่อแอปที่แสดงในระบบ, default "ContentFlow")
- logoDataUrl (รูปโลโก้ที่อัปโหลดเอง เก็บเป็น base64 data URL, default null = ใช้ตัวอักษรย่อ "CF" แทน)

## กติกาการไหลของข้อมูล (หัวใจของระบบ)
- ตัวเลขในแดชบอร์ด/สรุปรายเดือน/หน้าทีมงาน คำนวณจาก dailyReports เท่านั้น ห้ามพิมพ์ตัวเลขตรงๆ ที่อื่น
- "จำนวนชิ้นงานต่อคนต่อเดือน" = รวม จำนวนชิ้น ของ dailyReports ที่ memberId ตรงกัน และวันที่อยู่ในเดือนนั้น แยกย่อยตามประเภทงานได้
- เมื่อสร้างสคริปต์ใหม่ในคลังสคริปต์ ต้องเลือกผูกกับ contentPlan ที่มีอยู่ (หรือสร้าง contentPlan ใหม่คู่กัน) — ห้ามสคริปต์ลอยไม่มีคอนเทนต์อ้างอิง
- เปลี่ยนสถานะ contentPlan ได้จากหน้าแพลนคอนเทนต์เท่านั้น เรียงตามลำดับ ไอเดีย → เขียนสคริปต์ → ถ่ายทำ → ตัดต่อ → พร้อมลง → ลงแล้ว (ข้ามขั้นได้ถ้าจำเป็นจริง)
- ลบ member ที่มี dailyReports/contentPlans/scripts/ideas/workItems ผูกอยู่แล้วไม่ได้ (ระบบเช็คให้อัตโนมัติแล้วบล็อก) ให้เปลี่ยนสถานะเป็น "พ้นทีม" แทน เพื่อไม่ให้ตัวเลขย้อนหลังหาย — ลบถาวรได้เฉพาะสมาชิกที่ยังไม่มีข้อมูลผูกอยู่เลยเท่านั้น (เช่น เพิ่งเพิ่มผิดชื่อ)
- (แก้ไข — เดิมบังคับ) งานในแพลนงานรายวัน (workItems) ไม่บังคับต้องผูกกับแบรนด์อีกต่อไป — brandId ปล่อยว่างได้ ("อื่นๆ") งานที่ไม่มีแบรนด์จะไม่นับในสรุปแยกตามแบรนด์แต่ยังนับในยอดรวม
- สรุปงานทีม (รวม + แยกแบรนด์) บนแดชบอร์ดคำนวณจาก workItems.status เท่านั้น ห้ามพิมพ์ตัวเลขตรงๆ: งานที่กำลังทำ = สถานะ ดำเนินการ + แก้ไข + สำเร็จ, งานที่รอตรวจ = สถานะ รอตรวจ, งานที่โพสต์แล้ว = สถานะ Post (สถานะ "ยกเลิก" ไม่นับในสรุปทั้ง 3 กลุ่มเลย เพราะไม่ใช่งานที่ต้องติดตามต่อ)
- "ชิ้นงานแยกตามแบรนด์" บนแดชบอร์ดคำนวณจาก dailyReports.brandId เท่านั้น (รีพอร์ตที่ไม่ได้เลือกแบรนด์จะไม่นับในสรุปนี้ แต่ยังนับในยอดรวมทั้งหมดตามปกติ)
- (ตั้งแต่ TASK 33) ปฏิทินดึงข้อมูลจาก contentPlans.publishDate (คอนเทนต์ที่วางแผนลง), calendarEvents.date (เหตุการณ์ที่เพิ่มเอง), และรายการวันสำคัญ/วันนักขัตฤกษ์แบบ hardcode (THAI_FIXED_HOLIDAYS — เฉพาะวันที่ตายตัว ไม่รวมวันหยุดจันทรคติ) เท่านั้น — ไม่ดึงจาก workItems อีกต่อไป (ก่อนหน้านี้เคยดึงจาก workItems.date ด้วย ตัดออกตาม TASK 33)
- todayISO() (ใช้กำหนด "วันนี้" ทั้งระบบ — ไฮไลต์ในปฏิทิน, ค่าเริ่มต้นวันที่ในฟอร์มบันทึกงาน, เดือนปัจจุบันของแดชบอร์ด) ต้องคำนวณจากเวลาท้องถิ่นของเบราว์เซอร์ (`getFullYear`/`getMonth`/`getDate`) ห้ามใช้ `toISOString()` ตรงๆ เพราะแปลงเป็น UTC ทำให้ช่วงเที่ยงคืน-เจ็ดโมงเช้าตามเวลาไทย ระบบจะเข้าใจว่ายังเป็น "เมื่อวาน" อยู่ (บั๊กที่แก้ใน TASK 33)
- Product ในฟอร์มรายงานการทำงานต้องเลือกจากรายการสินค้าของแบรนด์ที่เลือกไว้เท่านั้น (brands[].products) ถ้าแบรนด์นั้นยังไม่มีสินค้าเลย ต้องไปเพิ่มที่หน้า "งานที่บริษัทรับผิดชอบ" ก่อน
- (ตั้งแต่ TASK 22) ฟอร์มบันทึกงานในหน้ารายงานการทำงานประจำวันสร้าง workItem และ dailyReport พร้อมกันเสมอ ผูกกันด้วย workItems.reportId — แก้ไขหรือลบฝั่งใดฝั่งหนึ่ง (จากตารางแพลนงานรายวัน หรือจากตารางรายวันที่หน้าสรุป KPI) ต้องอัปเดต/ลบอีกฝั่งที่ผูกกันไปด้วยเสมอ ห้ามให้ข้อมูลสองฝั่งไม่ตรงกัน
- กติกาการแสดงผลบนมือถือ (ตั้งแต่ TASK 32): ตารางข้อมูลทุกตารางในระบบ (แพลนงานรายวัน, แพลนคอนเทนต์, รีพอร์ตรายวัน, สรุป KPI รายเดือน, ประวัติการทำงาน) บนจอ ≤860px จะพับแต่ละแถวเป็นการ์ดคอลัมน์เดียวอัตโนมัติ (label ชิดซ้าย ค่าชิดขวา) แทนการเลื่อนตารางแนวนอน ยกเว้นตารางที่มีแค่ 2 คอลัมน์สั้นๆ (เช่น ประวัติการเข้าใช้งาน) ที่ยังเป็นตารางปกติเพราะไม่ล้นจอ — หน้าทีมงานใช้การ์ดสรุปย่อแบบเฉพาะของตัวเอง (ไม่ใช่กลไกพับตารางทั่วไป) เพราะข้อมูลมีหลายฟิลด์เกินไปจนพับแบบธรรมดาแล้วยังยาว

## หน้าจอแต่ละเมนู (ย่อ)
- **แดชบอร์ด**: (ตั้งแต่ TASK 31 อัดแน่นให้แคปหน้าจอเดียวเห็นครบ) แถบสถิติย่อ 6 ตัวในแถวเดียว (ชิ้นงานเดือนนี้/ลงแล้ว/ส่งรีพอร์ต/กำลังทำ/รอตรวจ/โพสต์แล้ว), การ์ดสรุปแบรนด์เป็นกริดการ์ดย่อ (2 คอลัมน์บนมือถือ) โชว์ชื่อแบรนด์+ช่อง TikTok+จำนวนงาน 3 สถานะ พร้อมลิงก์ไปหน้า "งานที่บริษัทรับผิดชอบ", การ์ด "ใครทำอะไรบ้าง (เดือนนี้)" เป็นกราฟแท่งรายคน (คลิกแถวเปิดแดชบอร์ดรายคนได้), การ์ดแยกตามแบรนด์เป็นกราฟแท่งสั้นๆ + แยกตามประเภทงานเป็นชิปคำเดียวเรียงแถว (ไม่ใช่ตาราง), รายการคอนเทนต์ที่ใกล้ถึงวันลงใน 7 วันข้างหน้าปิดท้าย
- **รายงานการทำงานประจำวัน**: (ตั้งแต่ TASK 32 เปลี่ยนฟอร์มบันทึกงานเร็วใหม่ทั้งหมด — เขียนทับดีไซน์ TASK 31) การ์ด "บันทึกงานวันนี้" เป็นรายการ "ก้อนงาน" ที่เพิ่มได้เรื่อยๆ ด้วยปุ่ม "+ เพิ่มงาน" — แต่ละก้อนมี วันที่ * (ก้อนใหม่พรีฟิลวันที่จากก้อนก่อนหน้าอัตโนมัติ ไม่ต้องพิมพ์ซ้ำถ้าวันเดียวกัน) และ ประเภทงาน * แล้วฟิลด์ที่เหลือเปลี่ยนไปตามประเภทงานที่เลือก:
  - **ตัดคลิป**: แบรนด์ (ไม่บังคับ — เลือก "อื่นๆ" ได้ถ้าไม่ผูกกับแบรนด์ไหน), รายละเอียด (ไม่บังคับ), จำนวน *, สถานะ (ดำเนินการ/รอตรวจ/แก้ไข/สำเร็จ/Post/ยกเลิก), รูปหน้าปก (อัปโหลดรูปจริง เก็บเป็น coverImageDataUrl — ไม่บังคับตอนบันทึกทั่วไป แต่ต้องมีถ้าจะให้คลิปนี้ไปโชว์ในหน้า "แพลนหน้าช่อง" ได้), ไดร์ฟงาน (บังคับก็ต่อเมื่อสถานะเป็น "รอตรวจ" หรือ "สำเร็จ" — label เปลี่ยนเป็น "ไดร์ฟงาน *" อัตโนมัติ), ช่องทางที่ลง (โผล่ขึ้น+บังคับเลือกเฉพาะเมื่อสถานะ = "Post" — เลือกจาก channels ของแบรนด์ที่เลือกไว้ ถ้าแบรนด์นั้นยังไม่มีช่องทางเลยจะเตือนให้ไปเพิ่มก่อน), ลิงค์โพสต์ (ขึ้นเฉพาะเมื่อสถานะ = "Post" และบังคับกรอก — บันทึกเป็น postDate/linkPost ของ workItem โดย postDate = วันที่เดียวกับก้อนงานนั้น)
  - **ออกกอง**: แบรนด์ (ไม่บังคับ), สถานที่ (ไม่บังคับ — เก็บใน workItems.location), รายละเอียด (ไม่บังคับ)
  - **พากย์เสียง**: แบรนด์ (ไม่บังคับ), จำนวน *
  - **ถ่ายฟุตเทจ / เขียนสคริปต์ / อื่นๆ**: แบรนด์ (ไม่บังคับ), รายละเอียด (ไม่บังคับ)
  - ทุกประเภทที่ไม่มีช่องจำนวนแสดงในฟอร์ม ระบบบันทึกจำนวน = 1 โดยอัตโนมัติ — ทีมงาน (ownerId) ไม่ต้องเลือกเอง ใช้ผู้ที่ล็อกอินอยู่ขณะนั้นเสมอ (ดู <เดาให้ — แก้ได้> ในหัวข้อ workItems ด้านบน) — แบรนด์ทุกประเภทงานไม่บังคับกรอกอีกต่อไป (เปลี่ยนจากเดิมที่บังคับเสมอ) เลือก "อื่นๆ" ในดรอปดาวน์ได้ถ้างานนั้นไม่ได้ผูกกับแบรนด์ไหน
  - กดปุ่ม "ลบ" ที่มุมขวาบนของแต่ละก้อนเพื่อเอาก้อนนั้นออกก่อนบันทึกได้ (เหลือก้อนเดียวจะลบไม่ได้ ระบบเติมก้อนว่างใหม่ให้อัตโนมัติแทน) — กด "💾 บันทึกทั้งหมด" ครั้งเดียวตรวจสอบทุกก้อนแล้วสร้าง workItem+dailyReport แยกคู่ต่อก้อน (คนละแบรนด์/ประเภทงานในคราวเดียวกันได้) สำเร็จแล้วฟอร์มรีเซ็ตกลับเหลือก้อนว่าง 1 ก้อนพร้อมบันทึกรอบถัดไป
  - ไม่มีช่อง Product ในฟอร์มนี้อีกต่อไป (ยังมีอยู่ในฟอร์มแก้ไขงานแบบเต็มที่ตาราง/ปฏิทิน)
  - ด้านล่างมีตารางรายงานการทำงานแบบละเอียด (มีฟอร์มแก้ไขงานแบบเต็ม รวม Product/สถานที่/สถานะ/รูปภาพ/ลิงก์ไดรฟ์ — ทุกฟิลด์ไม่บังคับตามเดิมจาก TASK 28) กรองตามแบรนด์/ผู้รับผิดชอบ/สถานะได้ แก้ไข/ลบจากตารางนี้จะอัปเดต/ลบรีพอร์ตที่ผูกกันไปด้วย — เปลี่ยนสถานะเป็น "Post" จากตารางหรือฟอร์มแก้ไขได้ทันที (ระบบถามวันที่โพสต์+ลิงก์โพสต์ให้ทันที) หน้านี้ไม่มีตารางรีพอร์ตย้อนหลังให้ดู (ดูที่เมนูสรุป KPI โหมดรายวัน) — ตารางนี้บนจอมือถือ (≤860px) พับเป็นการ์ดคอลัมน์เดียวต่อชิ้นงานอัตโนมัติเหมือนตารางอื่นๆ ในระบบ (ดูหัวข้อ "กติกาการแสดงผลบนมือถือ" ในหมวดกติกาการไหลของข้อมูลด้านบน)
- **ปฏิทิน**: (ตั้งแต่ TASK 33) ตารางปฏิทินรายเดือน 7 คอลัมน์ (อา-ส) เลื่อนดูเดือนก่อน/ถัดไปได้ แต่ละวันโชว์ 3 อย่าง: (1) ชื่อคอนเทนต์ที่วางแผนลง (จากแพลนคอนเทนต์ สีชิปตามสถานะ) (2) ป้ายวันสำคัญ/วันนักขัตฤกษ์ (ตัวอักษรแดงใต้เลขวันที่ ถ้าตรงกับวันในรายการ THAI_FIXED_HOLIDAYS) (3) ชิปเหตุการณ์ (สีม่วง จาก calendarEvents) — ปุ่ม "+ เพิ่มเหตุการณ์" ที่หัวเปิดฟอร์มเพิ่มเหตุการณ์ใหม่ (พรีฟิลวันนี้/วันที่ 1 ของเดือนที่ดูอยู่) และคลิกวันที่ว่างในตารางก็เปิดฟอร์มเพิ่มเหตุการณ์พรีฟิลวันนั้นได้เลย — คลิกชิปคอนเทนต์ที่มีอยู่แล้วเปิดฟอร์มแก้ไขคอนเทนต์นั้น คลิกชิปเหตุการณ์ที่มีอยู่แล้วเปิดฟอร์มแก้ไข/ลบเหตุการณ์นั้น — ไม่แสดงงานที่ทีมงานบันทึกรายวัน (workItems) แล้ว (ดูที่เมนู "รายงานการทำงานประจำวัน" แทน)
- **งานที่บริษัทรับผิดชอบ**: จัดการรายชื่อแบรนด์ (เพิ่ม/แก้ไข/ลบ) — ชื่อแบรนด์ + ช่อง TikTok + หมายเหตุ + รายชื่อสินค้าของแบรนด์นั้น (เพิ่ม/แก้ไข/ลบได้ในฟอร์มเดียวกัน) ใช้เป็นตัวเลือก Product ในหน้ารายงานการทำงานประจำวันทุกจุด ผลลัพธ์ไปโชว์สรุปที่หน้าแดชบอร์ดอัตโนมัติ (ตั้งแต่ TASK 31 เอาส่วนจัดการ "Format Type"/"Content Pillar" ออกแล้ว — ไม่ใช้สองฟิลด์นี้อีกต่อไป) — (ใหม่) เพิ่มส่วนจัดการ "ช่องทาง" ของแบรนด์นั้นในฟอร์มเดียวกัน (เพิ่ม/แก้ไข/ลบชื่อช่องทางได้อิสระ เช่น "TikTok หลัก", "TikTok ปั่น 1", "Shopee") ใช้เป็นตัวเลือกช่องทางที่ลงตอนบันทึกสถานะ Post ของงานตัดคลิป
- **แพลนหน้าช่อง**: เลือกแบรนด์ + ช่องทาง + เดือนที่จะวางแผน → เห็นตารางกริดรูปหน้าปกของคลิปตัดคลิป**ทุกคลิปของแบรนด์นั้น**ที่มี coverImageDataUrl แล้ว (ไม่กรองตามช่องทาง/สถานะโพสต์ของคลิป — ไม่ว่าจะยังไม่โพสต์หรือโพสต์ไปแล้วก็ขึ้นให้เลือกทั้งหมด คลิปที่โพสต์แล้วมี badge "โพสต์แล้ว" กำกับ) **หรือกด "+ อัปโหลดรูปเอง" เพื่อเพิ่มรูปเข้ากริดตรงๆ โดยไม่ต้องรอมีงานจากทีม** (ใส่โน้ต/แคปชั่นกำกับได้) ปุ่มลูกศรขึ้น/ลงจัดเรียงลำดับได้ (บันทึกเป็น feedPlanItems.order) เพื่อดูภาพรวมหน้าฟีดก่อนลงจริง — วางแผนล่วงหน้าได้หลายเดือนพร้อมกัน (สลับดูทีละเดือนได้เหมือนปฏิทิน) คลิกรูปเพื่อดูรายละเอียดงาน/แก้โน้ต/เอาออกจากแผนได้ — คลิปที่จัดลำดับไว้แล้วจะไม่ถูกเอาออกจากแผนอัตโนมัติแม้สถานะจริงจะเปลี่ยนเป็นโพสต์แล้วภายหลัง (เอาออกเองเท่านั้นถึงจะหาย)
- **แพลนคอนเทนต์**: มุมมองปฏิทินรายเดือน + ตารางรายการ กดเข้าไปดู/แก้สถานะ วันที่ลง ผู้รับผิดชอบได้
- **คลังสคริปต์**: ฟอร์มเขียนสคริปต์ตามช่อง Hook/เนื้อหา/CTA ที่ตายตัว + รายการสคริปต์ทั้งหมด ค้นหาได้
- **ไอเดีย & เทรนด์**: บันทึกไอเดีย/เทรนด์/แคปชั่นแบบสั้นๆ ติดแท็กประเภทได้ ดึงไปใช้ตอนสร้างแพลนคอนเทนต์ได้ง่าย
- **ทีมงาน**: รายชื่อสมาชิก + ข้อมูลพนักงาน (ตำแหน่ง, แผนก, รหัสพนักงาน, วันเกิด, เบอร์โทร, อีเมล) + สถานะ (เทสงาน/ทดลองงาน/ประจำ/พ้นทีม) เปลี่ยนได้ทันทีจาก dropdown ในแถวตาราง ไม่ต้องเปิดฟอร์มแก้ไข + การ์ดสรุปผลงานรายเดือนต่อคน (จำนวนชิ้นงานรวม แยกประเภท) + ปุ่ม "แดชบอร์ด" ต่อแถวเพื่อเข้าดูโปรไฟล์การทำงานของคนนั้นแบบละเอียด (ไม่ใช่เมนูหลักในไซด์บาร์ เข้าถึงได้จากหน้าทีมงานเท่านั้น): ยอดชิ้นงานเดือนนี้/สะสม/จำนวนสคริปต์, แนวโน้ม 6 เดือนล่าสุด, แยกตามประเภทงาน, รีพอร์ตล่าสุด, คอนเทนต์ที่รับผิดชอบ, สคริปต์ที่เขียน — (ตั้งแต่ TASK 32 — เขียนทับ card ทั่วไปของ TASK 31 ด้วยการ์ดเฉพาะที่ย่อกว่าเดิม) บนจอมือถือ (≤860px) ตารางสมาชิกซ่อนไปทั้งหมด แสดงการ์ดสรุปย่อต่อคนแทน (avatar+ชื่อ+ตำแหน่ง·แผนกบรรทัดเดียว+dropdown สถานะบนแถวเดียวกัน แล้วบรรทัดถัดมา "เข้าร่วมวันที่" กับ "ชิ้นงานเดือนนี้" คู่กัน ปิดท้ายด้วยปุ่มแดชบอร์ด/แก้ไข) แทนตารางแบบพับ label:value ทีละแถวที่ยังยาวเกินไป — จอกว้าง (>860px) ยังเป็นตารางปกติเหมือนเดิมทุกอย่าง
- **สรุป KPI**: สลับ 2 โหมดในหน้าเดียว — โหมด "รายเดือน" (ตั้งแต่ TASK 34): เลือกเดือน → การ์ด KPI 3 ใบ (ยอดชิ้นงานรวม+badge เทียบเดือนก่อน ↑/↓/เท่าเดิม %, เฉลี่ยชิ้นงาน/วันที่มีการบันทึก, คนทำงานเยอะสุด) → การ์ด "อันดับคนทำงาน" เรียงมาก→น้อยพร้อมเลขอันดับ (คลิกแถวเปิดแดชบอร์ดรายคนได้ กลับมาหน้านี้ถูกต้อง) → ตารางสรุปยอดชิ้นงานทุกประเภทต่อคนแบบละเอียดเดิม + ปุ่มออกรายงาน CSV · โหมด "รายวัน": เลือกวันที่ + กรองตามคน → ตารางรีพอร์ตของวันนั้น ลบรายการที่กรอกผิดได้ + ปุ่มออกรายงาน CSV
- **ประวัติการใช้งาน**: ตารางประวัติการเข้าใช้งาน (คน, วันที่-เวลาล็อกอิน) + ตารางประวัติการทำงาน (คน, วันที่-เวลา, โมดูล, สิ่งที่ทำ) แต่ละตารางมีปุ่มออกรายงาน CSV แยกกัน
- **หน้าเข้าสู่ระบบ**: เลือกชื่อเข้าใช้งานจากรายชื่อ + ปุ่มเดียว "จัดการโปรไฟล์ผู้ใช้" ด้านล่างสุด เปิด modal เพิ่ม/แก้ไข/ลบสมาชิก (ชื่อ/ตำแหน่ง/แผนก/รูป ฯลฯ) ได้ทันทีก่อนล็อกอิน
